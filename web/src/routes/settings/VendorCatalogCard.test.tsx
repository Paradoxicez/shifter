import { describe, it } from 'vitest';

describe('VendorCatalogCard (Surface 1, D-23..D-25, D-40)', () => {
  it.skip('renders DataTable with Vendor | Family | Capability | Version | Installed | Devices using | Status columns', () => {
    // filled in plan 07-05
  });
  it.skip('hides Family + Version columns at viewport <= 768px', () => {
    // filled in plan 07-05
  });
  it.skip('shows Update to v{X.Y.Z} button when status = update-available', () => {
    // filled in plan 07-05
  });
});
