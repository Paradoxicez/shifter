import { describe, it } from 'vitest'

// Implementation: Plan 23 (login-ui).
// Visual + copy assertions for the login screen — UX-02 in VALIDATION.md.
// Navy primary + Inter font come from the Tailwind v4 + shadcn theme set in
// Plan 06; English copy is the canonical brand voice from CONTEXT.md.
describe.skip('Login screen (Plan 23)', () => {
  it('renders shadcn navy primary CSS variable', () => {})
  it('uses Inter font family', () => {})
  it('has English copy "Sign in to Shifter"', () => {})
})
