/**
 * Placeholder — Task 1 of Plan 03-08 ships the route wiring; Task 3 replaces
 * this with the full detail page (identity + 24h stats + Edit/Decommission).
 */
export default function GatewayDetailPage() {
  return (
    <div className="flex flex-col gap-6 p-6">
      <h1 className="text-2xl font-semibold leading-8">Gateway</h1>
    </div>
  )
}
