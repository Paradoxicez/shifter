import { describe, it } from 'vitest';

describe('CompareView (Surface 5, D-16, D-37, D-38)', () => {
  it.skip('renders Compare entities / Compare time ranges mode toggle', () => {
    // filled in plan 07-12
  });
  it.skip('swaps Entity A and Entity B when swap button clicked', () => {
    // filled in plan 07-12
  });
});
