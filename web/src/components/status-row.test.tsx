import { describe, it } from 'vitest'

// Implementation: Plans 06 (frontend-shell) + 17 (test-connection).
// StatusRow is the visual primitive used by the install-wizard test-connection
// step; three states map to three different icons + colors per UI-SPEC.
describe.skip('StatusRow component (Plan 06/17)', () => {
  it('renders reachable state with green dot', () => {})
  it('renders unreachable state with red icon', () => {})
  it('renders skipped state with muted icon', () => {})
})
