import { describe, it } from 'vitest';

describe('CodecTestRunner panel (Surface 4, D-05..D-08, D-26..D-28)', () => {
  it.skip('renders Test Codec panel collapsed by default', () => {
    // filled in plan 07-08
  });
  it.skip('shows decoded JSON tree on successful test', () => {
    // filled in plan 07-08
  });
  it.skip('renders error panel with line/col on goja exception', () => {
    // filled in plan 07-08
  });
});
