{
  "_comment": "STUB FIXTURE — regenerated by Wave 4 via `pnpm exec playwright open --save-storage=./web/playwright/fixtures/admin-session.json http://localhost:8080` after logging in as the seeded admin user. The cookie value below is a placeholder; tests that depend on a *real* logged-in session will fail until regeneration. Do NOT commit a valid session secret to this file — Shifter sessions live in Postgres and the install seed generates ephemeral secrets.",
  "cookies": [
    {
      "name": "session",
      "value": "stub-admin-session-replace-via-playwright-save-storage",
      "domain": "localhost",
      "path": "/",
      "expires": -1,
      "httpOnly": true,
      "secure": false,
      "sameSite": "Lax"
    }
  ],
  "origins": []
}
