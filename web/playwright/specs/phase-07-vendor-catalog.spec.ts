import { test } from '@playwright/test';

test.describe('Phase 7 — Vendor Catalog flows', () => {
  test.skip('import from catalog: select Itron, prefill form, confirm', async () => {
    // filled in plan 07-14
  });
  test.skip('apply catalog update with per-field "use mine"/"use catalog" toggles', async () => {
    // filled in plan 07-14
  });
  test.skip('run codec test, see decoded JSON + canonical mapping', async () => {
    // filled in plan 07-14
  });
});
