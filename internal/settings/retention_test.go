package settings

import "testing"

func TestRetentionConfigCRUD(t *testing.T) {
	t.Skip("Plan 05-11 Task 1: PATCH /api/settings/retention writes retention_config; range guarded per UI-SPEC; pgx-only (no lib/pq)")
}
