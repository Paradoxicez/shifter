---
phase: 260513-p71
plan: 01
subsystem: backup-wiring, alerts, floor-plan
tags: [bugfix, uat-regression, auth, enum, url]
dependency_graph:
  requires: []
  provides: [backup-routes-mounted, offline_gateway-enum-correct, floor-plan-sidebar-url-correct]
  affects: [internal/cli/serve.go, internal/http/router_backup_test.go, web/src/routes/settings/AddRuleDialog.tsx, web/playwright/specs/alert-rule-gateway-offline.spec.ts, web/src/components/floor-plan/FloorPlanTab.tsx, web/playwright/specs/floor-plan-sidebar-url.spec.ts]
tech_stack:
  added: []
  patterns: [nil-guard wiring, Playwright API spec, Go router unit test without DB]
key_files:
  created:
    - internal/http/router_backup_test.go
    - web/playwright/specs/alert-rule-gateway-offline.spec.ts
    - web/playwright/specs/floor-plan-sidebar-url.spec.ts
  modified:
    - internal/cli/serve.go
    - web/src/routes/settings/AddRuleDialog.tsx
    - web/src/components/floor-plan/FloorPlanTab.tsx
decisions:
  - Used scope_kind='global' (not 'install') in Playwright spec to match actual API validation
  - Used cooldown_seconds (not threshold_minutes) to match createRuleRequest struct
metrics:
  duration: ~18 minutes
  completed: 2026-05-13
  tasks_completed: 3
  files_changed: 6
---

# Phase 260513-p71 Plan 01: Fix 3 High UAT Issues — BackupDeps Wiring Summary

One-liner: Wired BackupDeps into serve.go so /api/backup/* routes are auth-gated, corrected offline_gateway enum in AddRuleDialog, and fixed FloorPlanTab URL from /api/sites/{id}/devices to /api/devices/by-site/{id}.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Wire BackupDeps in serve.go + router unit test | 6054ca2 | internal/cli/serve.go, internal/http/router_backup_test.go |
| 2 | Fix gateway_offline → offline_gateway + Playwright spec | 55d15bc | web/src/routes/settings/AddRuleDialog.tsx, web/playwright/specs/alert-rule-gateway-offline.spec.ts |
| 3 | Fix FloorPlanTab URL + Playwright spec | 5e12710 | web/src/components/floor-plan/FloorPlanTab.tsx, web/playwright/specs/floor-plan-sidebar-url.spec.ts |

## Verification Results

- `go test ./internal/http/... -race -count=1` — 33 passed including new TestBackupRoutes_UnauthenticatedGetsReject
- `go build ./...` — success
- `cd web && pnpm tsc --noEmit` — no errors
- Both Playwright specs pass (2/2)
- `curl http://localhost:8080/api/backup/list` (no cookie) → 401 (was 200 HTML)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed wrong scope_kind and field name in Playwright spec**
- **Found during:** Task 2 verify (422 returned instead of 201)
- **Issue:** Plan's spec used `scope_kind: 'install'` (invalid) and `threshold_minutes: 30` (non-existent field). The API handler `validateScopeKindScopeID` only accepts `'global'`, `'metering_point'`, `'site'`, `'device'`, `'gateway'`. The `createRuleRequest` struct has `cooldown_seconds`, not `threshold_minutes`.
- **Fix:** Changed spec to `scope_kind: 'global'` and `cooldown_seconds: 1800`
- **Files modified:** web/playwright/specs/alert-rule-gateway-offline.spec.ts
- **Commit:** 55d15bc (same task commit)

## Known Stubs

None.

## Threat Flags

None beyond what was already in the plan's threat model. Task 1 wiring activates the existing `auth.RequireAction(ActionBackupRead/ActionBackupRun)` guards inside `backup.RegisterRoutes` — no new surface introduced.

## Self-Check: PASSED

- internal/cli/serve.go — modified (backupDeps block inserted, BackupDeps wired)
- internal/http/router_backup_test.go — created
- web/src/routes/settings/AddRuleDialog.tsx — modified ('offline_gateway')
- web/playwright/specs/alert-rule-gateway-offline.spec.ts — created
- web/src/components/floor-plan/FloorPlanTab.tsx — modified (/api/devices/by-site/)
- web/playwright/specs/floor-plan-sidebar-url.spec.ts — created
- Commits 6054ca2, 55d15bc, 5e12710 — all present in git log
