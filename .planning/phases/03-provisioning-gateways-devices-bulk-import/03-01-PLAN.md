---
phase: 03-provisioning-gateways-devices-bulk-import
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - internal/testsupport/chirpstack_mock.go
  - internal/testsupport/postgres_container.go
  - internal/testsupport/eui_fixtures.go
  - internal/testsupport/xlsx_fixtures.go
  - web/playwright.config.ts
  - web/package.json
  - web/tests/fixtures/import-template.xlsx
  - web/playwright/fixtures/admin-session.json
  - web/playwright/fixtures/viewer-session.json
  - internal/chirpstack/gateway_test.go
  - internal/chirpstack/gateway_metrics_cache_test.go
  - internal/api/gateways_handler_test.go
  - internal/api/gateways_decommission_test.go
  - internal/api/devices_list_test.go
  - internal/api/devices_add_test.go
  - internal/api/devices_bulk_decommission_test.go
  - internal/api/devices_reveal_test.go
  - internal/import/euikeys_test.go
  - internal/import/parser_xlsx_test.go
  - internal/import/parser_csv_test.go
  - internal/import/dryrun_test.go
  - internal/import/commit_test.go
  - internal/import/template_test.go
  - internal/import/job_ttl_test.go
  - internal/audit/bulk_import_envelope_test.go
  - internal/auth/can_phase3_test.go
  - internal/db/migrations_test.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every Phase 3 production task has a compile-able test file referencing it"
    - "excelize/v2 and singleflight (optional) are in go.mod"
    - "ChirpStack gRPC mock supports GatewayService.GetMetrics + DeviceService.GetKeys + DeviceService.GetActivation"
    - "Playwright is installed and admin/viewer session fixtures exist"
  artifacts:
    - path: "go.mod"
      contains: "github.com/xuri/excelize/v2"
    - path: "internal/testsupport/chirpstack_mock.go"
      provides: "in-process gRPC mock extending Phase 2 mock with Gateway* + GetKeys + GetActivation + GetMetrics"
    - path: "internal/testsupport/xlsx_fixtures.go"
      provides: "happy_5_rows, mixed_10, intra_dup, already_exists, malformed_eui byte-slice generators"
    - path: "web/playwright/fixtures/admin-session.json"
      provides: "pre-authenticated admin session for Playwright specs"
    - path: "web/playwright/fixtures/viewer-session.json"
      provides: "viewer session for RBAC negative tests"
  key_links:
    - from: "all wave-1..4 production tasks"
      to: "test files created here"
      via: "go test ./internal/{pkg} -run TestX exits non-zero (MISSING) until production lands"
      pattern: "TestX exists with t.Skip(\"awaiting wave-N implementation\")"
---

<objective>
Phase 3 Wave 0: lay down the test scaffolding so every later task has a `<verify>` target before production code exists. Adds the one new Go dependency (`excelize/v2`), extends the Phase 2 testsupport package with the gRPC mock methods Phase 3 needs (Gateway* + GetKeys + GetActivation + GetMetrics), and creates skeleton (`t.Skip` placeholder) test files for every package Phase 3 touches.

Purpose: Nyquist compliance — sampling MUST be possible from task 1 onward without "no test file yet" gaps.
Output: 1 dep added, 6 testsupport helpers, 16 skeleton Go test files, 2 Playwright fixtures, 1 golden XLSX template fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-provisioning-gateways-devices-bulk-import/03-CONTEXT.md
@.planning/phases/03-provisioning-gateways-devices-bulk-import/03-RESEARCH.md
@.planning/phases/03-provisioning-gateways-devices-bulk-import/03-VALIDATION.md
@CLAUDE.md
@go.mod
@web/package.json
@internal/testsupport
@internal/audit/log.go
</context>

<interfaces>
<!-- Phase 2 mock surface extended in Wave 0. -->
From internal/testsupport (Phase 2):
- `func StartPostgresContainer(t *testing.T) *PostgresContainer` — reusable; do not rewrite.
- `type ChirpStackMock struct { ... }` — Phase 2 mock with tenant/application/device-profile/device service registration.

New Phase 3 mock methods to add (in-process gRPC server registration via bufconn):
- `GatewayService.Create / Get / Update / Delete / List / GetMetrics`
- `DeviceService.GetKeys` (OTAA) / `GetActivation` (ABP)

Excelize/v2 v2.10.x reference:
- `excelize.OpenReader(io.Reader) (*File, error)`
- `(*File).GetRows(sheet string) ([][]string, error)`
- `excelize.NewFile()`, `(*File).SetCellValue`, `(*File).SetCellStr`, `(*File).NewStyle`, `(*File).AddDataValidation`, `(*File).AddComment`
- DevEUI text-format trick: `excelize.Style{NumFmt: 49}` (=`@`/text) on column.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add excelize/v2 + golang.org/x/sync (singleflight) deps + Playwright install</name>
  <files>go.mod, go.sum, web/package.json, web/playwright.config.ts</files>
  <read_first>
    - go.mod (verify Go 1.25.0; verify chirpstack-api/go/v4 v4.17.0 present)
    - web/package.json (verify pnpm 10.33.2, react-router-dom v7, @tanstack/react-table v8.21.3)
    - .planning/phases/03-provisioning-gateways-devices-bulk-import/03-RESEARCH.md §Environment Availability
  </read_first>
  <action>
    1. Run `go get github.com/xuri/excelize/v2@v2.10.0` (or latest 2.10.x). RESEARCH §Excelize confirms 2.10 requires Go 1.24+; the project is on 1.25.0.
    2. Run `go get golang.org/x/sync@latest` to enable the `singleflight` subpackage for the gateway metrics cache. Pure-Go, no CGo.
    3. Run `go mod tidy` to sort go.sum.
    4. Install Playwright: `cd web && pnpm add -D @playwright/test@latest && pnpm exec playwright install --with-deps chromium`.
    5. Create `web/playwright.config.ts` with: testDir=`./playwright/specs`, baseURL=`http://localhost:8080`, projects=[{name:'chromium', use:{...devices['Desktop Chrome'], storageState: undefined}}, {name:'admin', use:{storageState:'./playwright/fixtures/admin-session.json'}}, {name:'viewer', use:{storageState:'./playwright/fixtures/viewer-session.json'}}], reporter='html'. Use defineConfig from `@playwright/test`.
    6. Commit deps + config in one commit.
  </action>
  <acceptance_criteria>
    - `grep -q "github.com/xuri/excelize/v2 v2\\.10" go.mod` exits 0
    - `grep -q "golang.org/x/sync" go.mod` exits 0
    - `grep -q "@playwright/test" web/package.json` exits 0
    - `test -f web/playwright.config.ts` exits 0
    - `go build ./...` exits 0 (proves new dep links)
    - `cd web && pnpm exec playwright --version` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && grep -q "github.com/xuri/excelize/v2" go.mod && grep -q "@playwright/test" web/package.json && test -f web/playwright.config.ts</automated>
  </verify>
  <done>Three new deps land in go.mod / web/package.json; Playwright config exists; project still builds.</done>
</task>

<task type="auto">
  <name>Task 2: Extend testsupport with Phase 3 mock surface + XLSX/EUI fixtures</name>
  <files>internal/testsupport/chirpstack_mock.go, internal/testsupport/postgres_container.go, internal/testsupport/eui_fixtures.go, internal/testsupport/xlsx_fixtures.go, web/tests/fixtures/import-template.xlsx, web/playwright/fixtures/admin-session.json, web/playwright/fixtures/viewer-session.json</files>
  <read_first>
    - internal/testsupport/ existing files (reuse `StartPostgresContainer` + Phase 2 `ChirpStackMock`)
    - internal/chirpstack/device.go (device.pb.go shapes for GetDeviceKeys / GetDeviceActivation responses)
    - .planning/phases/03-provisioning-gateways-devices-bulk-import/03-RESEARCH.md §ChirpStack v4 GatewayService and §Excelize/v2
    - .planning/phases/03-provisioning-gateways-devices-bulk-import/03-VALIDATION.md §Wave 0 Requirements
  </read_first>
  <action>
    1. **chirpstack_mock.go** — extend the Phase 2 mock struct with five `gateways map[string]*api.Gateway`, `gatewayMetrics map[string]*api.GetGatewayMetricsResponse`, `deviceKeys map[string]*api.DeviceKeys`, `deviceActivations map[string]*api.DeviceActivation` fields (all keyed by lowercase EUI/UUID). Implement the 6 GatewayService RPCs (Create/Get/Update/Delete/List/GetMetrics) + 2 DeviceService RPCs (GetKeys/GetActivation) following the exact request/response types from RESEARCH §ChirpStack v4 GatewayService. Each method:
      - Locks `mu sync.Mutex`.
      - Looks up by request key; returns `status.Errorf(codes.NotFound, ...)` on miss (so handler can `errors.Is(err, ErrNotFound)` distinguish).
      - For `GetMetrics`: returns whatever was injected via `SetGatewayMetrics(gatewayId, rx, tx, ok []float32)` helper; default = 24 zeros for each series.
      - Add `RegisterServices(s *grpc.Server)` extension that calls `api.RegisterGatewayServiceServer(s, mock)` in addition to the Phase 2 registrations.
    2. **eui_fixtures.go** — exported `var` block:
      - `ValidDevEUI1 = "70b3d59999000001"`, `ValidDevEUI2 = "70b3d59999000002"`, ... up to 5
      - `ValidGatewayID1 = "ac1f09fffe000001"`
      - `ValidAppKey = "00112233445566778899aabbccddeeff"` (32 hex)
      - `ValidDevAddr = "01020304"` (8 hex)
      - `MalformedEUI = "70b3d5XYZ9000001"` (contains non-hex)
      - `ShortEUI = "70b3d5"` (length fail)
      - `MixedCaseEUI = "70B3D59999000001"` (should normalize lowercase)
      - `WithSeparators = "70:b3:d5:99:99:00:00:01"` (should strip)
    3. **xlsx_fixtures.go** — five exported funcs returning `[]byte` of in-memory XLSX:
      - `HappyFiveRows()`: 5 valid OTAA rows with dev_eui, name, device_profile, site_id, activation_mode=OTAA, join_eui, app_key
      - `MixedTenRows()`: 5 valid + 5 invalid (mix of bad-EUI, missing site, dup-in-file)
      - `IntraFileDuplicate()`: 3 rows where row 2 and row 3 share dev_eui (testing D-08 intra-file dup detection)
      - `AlreadyExistsFixture(seedEUI string)`: 2 rows where row 1 dev_eui == seedEUI (testing D-06 already_exists outcome)
      - `MalformedEUIFixture()`: 2 rows where row 1 dev_eui is `MalformedEUI`
      - Each helper uses `excelize.NewFile()`, writes "Devices" sheet with proper headers from the import schema (D-04), then `f.WriteToBuffer()` returns the bytes.
    4. **import-template.xlsx golden file** — generate via a `go run` helper that emits the canonical template (headers + tooltips + data validation dropdown on activation_mode + NumFmt=49 text format on DevEUI col). Save bytes to `web/tests/fixtures/import-template.xlsx`. Test in Task 3 round-trips this through the parser.
    5. **admin-session.json + viewer-session.json** — Playwright `storageState` JSON shape: `{cookies: [{name: "session", value: "...", domain: "localhost", path: "/", ...}], origins: []}`. Stub values for now — Wave 4 Playwright tasks regenerate by running `pnpm exec playwright open --save-storage` against a seeded dev server. Initial files contain comment block explaining regeneration.
    6. Compile and verify: `go build ./internal/testsupport/...` exits 0.
  </action>
  <acceptance_criteria>
    - `grep -q "RegisterGatewayServiceServer" internal/testsupport/chirpstack_mock.go` exits 0
    - `grep -q "GetGatewayMetricsResponse" internal/testsupport/chirpstack_mock.go` exits 0
    - `grep -q "GetDeviceKeys" internal/testsupport/chirpstack_mock.go` exits 0
    - `grep -q "GetDeviceActivation" internal/testsupport/chirpstack_mock.go` exits 0
    - `grep -q "ValidDevEUI1" internal/testsupport/eui_fixtures.go` exits 0
    - `grep -q "MalformedEUI" internal/testsupport/eui_fixtures.go` exits 0
    - `grep -q "IntraFileDuplicate\|HappyFiveRows" internal/testsupport/xlsx_fixtures.go` exits 0
    - `test -f web/tests/fixtures/import-template.xlsx` exits 0
    - `test -f web/playwright/fixtures/admin-session.json` exits 0
    - `test -f web/playwright/fixtures/viewer-session.json` exits 0
    - `go build ./internal/testsupport/...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./internal/testsupport/... && test -f web/tests/fixtures/import-template.xlsx && test -f web/playwright/fixtures/admin-session.json && grep -q "RegisterGatewayServiceServer" internal/testsupport/chirpstack_mock.go && grep -q "GetDeviceActivation" internal/testsupport/chirpstack_mock.go</automated>
  </verify>
  <done>Phase 3 testsupport package compiles with all new mock + fixture helpers; golden XLSX template fixture exists.</done>
</task>

<task type="auto">
  <name>Task 3: Create 16 skeleton test files (one per production target) with t.Skip("Wave N — awaiting impl")</name>
  <files>internal/chirpstack/gateway_test.go, internal/chirpstack/gateway_metrics_cache_test.go, internal/api/gateways_handler_test.go, internal/api/gateways_decommission_test.go, internal/api/devices_list_test.go, internal/api/devices_add_test.go, internal/api/devices_bulk_decommission_test.go, internal/api/devices_reveal_test.go, internal/import/euikeys_test.go, internal/import/parser_xlsx_test.go, internal/import/parser_csv_test.go, internal/import/dryrun_test.go, internal/import/commit_test.go, internal/import/template_test.go, internal/import/job_ttl_test.go, internal/audit/bulk_import_envelope_test.go, internal/auth/can_phase3_test.go, internal/db/migrations_test.go</files>
  <read_first>
    - .planning/phases/03-provisioning-gateways-devices-bulk-import/03-VALIDATION.md §Required Test Files (full list of expected test function names)
    - internal/db/migrations_test.go (existing — extend, do NOT overwrite; add Phase 3 migration assertions)
  </read_first>
  <action>
    For each test file in the files list, create a Go source file with:
    - `package {pkg}` matching directory.
    - Imports of `testing`, project imports as needed.
    - One or more `func TestX(t *testing.T) { t.Skip("Wave N: awaiting implementation per 03-VALIDATION.md") }` stubs whose names match the canonical names in 03-VALIDATION.md. Map (one test per VALIDATION row):
      - `gateway_test.go`: TestCreateGateway, TestGetGateway, TestUpdateGateway, TestDeleteGateway, TestListGateways, TestGetMetrics
      - `gateway_metrics_cache_test.go`: TestMetricsCache_TTL, TestMetricsCache_SingleFlight
      - `gateways_handler_test.go`: TestListGatewaysHandler, TestCreateGatewayHandler_RegionDefault, TestUpdateGatewayHandler, TestArchiveGateway
      - `gateways_decommission_test.go`: TestDecommissionGateway_SoftDelete, TestDecommissionGateway_24hWarning, TestRestoreGateway
      - `devices_list_test.go`: TestListDevicesFiltered_Site, TestListDevicesFiltered_Status, TestListDevicesFiltered_LastSeen, TestListDevicesFiltered_TextSearch, TestListDevicesSorted, TestListDevicesPaginated
      - `devices_add_test.go`: TestAddDevice_OTAA, TestAddDevice_ABP, TestAddDevice_ABP_FCntCarryOver
      - `devices_bulk_decommission_test.go`: TestBulkDecommissionDevices_PartialSuccess
      - `devices_reveal_test.go`: TestRevealSecrets_AdminOTAA, TestRevealSecrets_AdminABP, TestRevealSecrets_Viewer403, TestRevealSecrets_NotFound, TestRevealSecrets_CSUnreachable502, TestRevealSecrets_NoCredentials409, TestRevealSecrets_AuditNoSecretMaterial
      - `euikeys_test.go`: TestNormalizeDevEUI, TestNormalizeJoinEUI, TestNormalizeAppKey, TestNormalizeDevAddr, TestNormalizeNwkSKey, TestNormalize_StripPrefixesAndSeparators
      - `parser_xlsx_test.go`: TestParseXLSX_HappyPath, TestParseXLSX_DataValidationRoundtrip
      - `parser_csv_test.go`: TestParseCSV_UTF8BOM, TestParseCSV_NonUTF8Reject, TestParseCSV_BOMStrip
      - `dryrun_test.go`: TestDryRun_FormatErrors, TestDryRun_IntraFileDuplicate, TestDryRun_PreExistingAlreadyExists, TestDryRun_MissingSite, TestDryRun_MissingProfile, TestDryRun_RequiredFields, TestDryRun_KeyLengthPerMode
      - `commit_test.go`: TestCommit_HappyPath, TestCommit_PartialCommit, TestCommit_Idempotent, TestCommit_AuditEnvelopeRow
      - `template_test.go`: TestGenerateTemplate_Headers, TestGenerateTemplate_DevEUITextFormat, TestGenerateTemplate_ActivationDropdown, TestGenerateTemplate_RoundTrip
      - `job_ttl_test.go`: TestImportJob_TTL1Hour, TestImportJob_ExpiredOnRead
      - `bulk_import_envelope_test.go`: TestBulkImport_PerDeviceAndEnvelope, TestBulkImport_RequestIDGrouping
      - `can_phase3_test.go`: TestCan_GatewayCreate_AdminOK_ViewerForbidden, TestCan_GatewayUpdate_AdminOK_ViewerForbidden, TestCan_GatewayArchive_AdminOK_ViewerForbidden, TestCan_GatewayRestore_AdminOK_ViewerForbidden, TestCan_DeviceBulkImport_AdminOK_ViewerForbidden, TestCan_DeviceRevealSecrets_AdminOK_ViewerForbidden
      - `migrations_test.go` (EXTEND existing — add Phase 3 cases): TestPhase3Migrations_0018_Gateway_Apply, TestPhase3Migrations_0018_Down, TestPhase3Migrations_0019_ImportJob_Apply, TestPhase3Migrations_0019_Down, TestPhase3Migrations_0020_AuditLogVocabulary_Apply, TestPhase3Migrations_0020_AcceptsNewActionsAndEntityTypes
    - Each test body: `t.Skip("Wave N — implementation pending; see 03-VALIDATION.md row {row}")`. Pick N per RESEARCH §Recommended Wave Breakdown.
    - For `migrations_test.go` Phase 3 additions: keep existing tests intact; append new `func TestPhase3Migrations_*` blocks at the bottom.
  </action>
  <acceptance_criteria>
    - For every test name listed in 03-VALIDATION.md §Required Test Files: `grep -rq "func {TestName}(" internal/` exits 0
    - `go vet ./internal/...` exits 0 (compiles)
    - `go test ./internal/... -run "TestCan_GatewayCreate_AdminOK_ViewerForbidden" -count=1` runs and SKIPS (exit 0, with "SKIP" in output)
    - `go test ./internal/import/... -count=1` runs and skips every test (no panic, exit 0)
  </acceptance_criteria>
  <verify>
    <automated>go vet ./internal/... && go test ./internal/import/... ./internal/chirpstack/... ./internal/audit/... ./internal/auth/... -run TestPhase3 -count=1 2>&1 | grep -q "SKIP\|ok"</automated>
  </verify>
  <done>All 18 skeleton test files compile; running `go test ./internal/...` produces SKIP output for every Phase 3 test; subsequent Wave 1+ tasks know which tests to make green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — pure test scaffolding) | Wave 0 ships test files, fixtures, and one dependency. No new runtime trust boundaries are introduced; Phase 3 boundaries (operator → /api/devices/:eui/keys, operator → /api/imports, operator → /api/gateways) all enter the trust model in Waves 2+ when handlers ship. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verified By |
|-----------|----------|-----------|----------|-------------|-----------------|-------------|
| T-3-01 | T (Tampering) | excelize/v2 dependency chain | low | mitigate | Pin to `v2.10.0` exact (no `^` semver); `go mod verify` in CI catches checksum drift; excelize is pure-Go (no CGo) so no native binary supply-chain risk | go.sum review |
| T-3-02 | I (Information Disclosure) | testsupport fixtures (admin/viewer session JSON) | low | accept | Fixtures contain stub session IDs only; real sessions regenerated by Wave 4 against ephemeral dev server. README in `web/playwright/fixtures/` explains the rotation expectation. **NOT committed with valid session secrets** | manual review in Task 2 |
</threat_model>

<verification>
- `go build ./...` and `go vet ./internal/...` both exit 0
- All test files listed in 03-VALIDATION.md §Required Test Files exist and contain matching `func Test*` declarations
- `go test ./internal/... -count=1` exits 0 with SKIP markers (no FAIL, no missing-symbol errors)
- `cd web && pnpm exec playwright --version` returns a version string
</verification>

<success_criteria>
- 03-VALIDATION.md row "File Exists" column flips from ❌ → 🟡 (file exists, test skipped) for all Phase 3 rows
- Subsequent Wave 1+ plans can reference test names as `<automated>` targets with confidence that the file exists
- No production code shipped — pure scaffolding plan
</success_criteria>

<output>
After completion, create `.planning/phases/03-provisioning-gateways-devices-bulk-import/03-01-SUMMARY.md` documenting:
- Exact versions pinned (excelize/v2 @ vX.Y.Z, golang.org/x/sync @ vX.Y.Z, @playwright/test @ vX.Y.Z)
- List of test files created with their wave-N target
- testsupport API surface added (function signatures for mock extension)
- Any deviation from this plan (e.g. test renames) and rationale
</output>
