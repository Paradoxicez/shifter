---
phase: 03-provisioning-gateways-devices-bulk-import
plan: 09
type: execute
wave: 6
depends_on: [01, 02, 05, 06, 07, 08]
files_modified:
  - web/src/lib/imports.ts
  - web/src/routes/devices/index.tsx
  - web/src/routes/devices/index.test.tsx
  - web/src/routes/devices/search-params.ts
  - web/src/routes/devices/search-params.test.ts
  - web/src/routes/devices/filter-toolbar.tsx
  - web/src/routes/devices/bulk-action-bar.tsx
  - web/src/routes/devices/bulk-decommission-dialog.tsx
  - web/src/routes/devices/bulk-decommission-dialog.test.tsx
  - web/src/routes/devices/bulk-import-dialog.tsx
  - web/src/routes/devices/bulk-import-dialog.test.tsx
  - web/src/routes/admin/imports/index.tsx
  - web/src/routes/admin/imports/index.test.tsx
  - web/src/routes/admin/imports/$jobId.tsx
  - web/src/routes/admin/imports/$jobId.test.tsx
  - web/src/App.tsx
  - web/src/components/sidebar/nav-items.tsx
autonomous: true
requirements: [DEV-01, DEV-06, DEV-07, DEV-08, UX-03]
must_haves:
  truths:
    - "Operator can filter /devices via URL params (site multi-select, status, last_seen window, q text-search) and the URL deep-links/back-forwards correctly via react-router-dom v7 useSearchParams + zod schema"
    - "Page-size selector (25/50/100) + offset pagination with prev/next + 'Page N of M' label"
    - "Sort by clicking column header (name / dev_eui / site / last_seen / created_at) toggles asc/desc indicator + updates URL"
    - "Bulk-select via row checkboxes; sticky bulk-action bar appears with N selected + Decommission + Clear selection"
    - "Bulk decommission AlertDialog confirms then calls POST /api/devices/bulk-decommission"
    - "Bulk import dialog: 3-step (Upload → Preview → Commit) — file picker + Download template button → POST /api/imports → preview banner + TanStack Table with expandable per-row reason → button-only Commit"
    - "Imports admin page /admin/imports lists prior jobs with summary counters"
    - "Import job detail page /admin/imports/:jobId renders per-row TanStack Table + Download errors.xlsx button"
    - "URL params use react-router-dom v7 useSearchParams (NOT TanStack Router — CORRECTION per RESEARCH)"
    - "No 'tenant' / 'application' text anywhere in any of the new files"
  artifacts:
    - path: "web/src/routes/devices/search-params.ts"
      provides: "devicesSearchSchema (zod) + useDevicesSearch hook"
    - path: "web/src/routes/devices/bulk-import-dialog.tsx"
      provides: "3-step ResponsiveDialog with file picker + preview table + commit confirm"
    - path: "web/src/routes/admin/imports/$jobId.tsx"
      provides: "Per-row outcomes table + Download errors.xlsx button"
  key_links:
    - from: "URL ?site=A&site=B&status=active&last_seen=24h&q=foo&sort=-last_seen&page=2&per_page=50"
      to: "GET /api/devices?site=A&site=B&..."
      via: "useDevicesSearch parses zod schema, passes to listDevices() query, useQuery re-fetches on URL change"
      pattern: "useSearchParams\\(\\)|useDevicesSearch\\(\\)"
    - from: "Bulk import file upload"
      to: "POST /api/imports multipart"
      via: "FormData + fetch with credentials"
      pattern: "FormData\\(\\)"
---

<objective>
Phase 3 Wave 4: ship the Devices list filter/sort/pagination UI, bulk-decommission flow, Bulk Import 3-step dialog, and Imports admin pages. URL state via react-router-dom v7 useSearchParams + zod (NOT TanStack Router — research corrected the CONTEXT D-15 wording).

Purpose: DEV-01 (full filter/sort/page surface), DEV-06 (bulk import), DEV-07 (idempotency visible to operator), DEV-08 (template download), D-36 (imports admin scoped to job detail — no general audit UI yet).
Output: 2 new pages (admin/imports + $jobId), extended /devices page with filter toolbar + bulk-action bar, 2 new dialogs (bulk decommission, bulk import 3-step), sidebar nav, comprehensive vitest coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-provisioning-gateways-devices-bulk-import/03-CONTEXT.md
@.planning/phases/03-provisioning-gateways-devices-bulk-import/03-RESEARCH.md
@.planning/phases/03-provisioning-gateways-devices-bulk-import/03-UI-SPEC.md
@CLAUDE.md
@web/src/lib/devices.ts
@web/src/routes/devices/index.tsx
@web/src/App.tsx
</context>

<interfaces>
From Plan 03-05 (Wave 2 — landed):
```
POST   /api/imports              — multipart upload + dry-run
POST   /api/imports/:job_id/commit
GET    /api/imports              — list jobs (paginated)
GET    /api/imports/:job_id      — job + rows
GET    /api/imports/template.xlsx
GET    /api/imports/:job_id/errors.xlsx
```

From Plan 03-06 (Wave 3 — landed):
```
GET    /api/devices?site=&site=&status=&last_seen=&q=&sort=&page=&per_page=  — filtered list
POST   /api/devices/bulk-decommission                                         — body {device_ids: [], reason: ""}
```

Devices URL state zod schema (per RESEARCH §react-router-dom v7 URL State):
```typescript
export const devicesSearchSchema = z.object({
  site: z.array(z.string().uuid()).optional().default([]),
  status: z.enum(['active', 'inactive', 'never_joined']).optional(),
  last_seen: z.enum(['24h', '7d', '30d', 'all']).optional().default('all'),
  q: z.string().optional().default(''),
  page: z.coerce.number().int().min(1).default(1),
  per_page: z.union([z.literal(25), z.literal(50), z.literal(100)]).default(50),
  sort: z.enum(['name', '-name', 'dev_eui', '-dev_eui', 'site', '-site', 'last_seen', '-last_seen', 'created_at', '-created_at']).default('-last_seen'),
})
export type DevicesSearch = z.infer<typeof devicesSearchSchema>
```

URL multi-value pattern: `site=uuid1&site=uuid2` (URLSearchParams.append + getAll). NEVER comma-separated.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: URL state hook + extended devices list with filter toolbar + sort + pagination</name>
  <files>web/src/routes/devices/search-params.ts, web/src/routes/devices/search-params.test.ts, web/src/routes/devices/filter-toolbar.tsx, web/src/routes/devices/index.tsx, web/src/routes/devices/index.test.tsx</files>
  <read_first>
    - web/src/routes/devices/index.tsx (Phase 2 minimal version — extend, do NOT delete)
    - .planning/phases/03-provisioning-gateways-devices-bulk-import/03-UI-SPEC.md §Devices list page extended (filter toolbar, bulk-action bar, sort header behavior)
    - .planning/phases/03-provisioning-gateways-devices-bulk-import/03-RESEARCH.md §react-router-dom v7 URL State for Filters (full schema + hook implementation)
  </read_first>
  <behavior>
    Tests in `search-params.test.ts`:
    - TestZodSchema_DefaultsApplied — empty params → {site:[], last_seen:'all', q:'', page:1, per_page:50, sort:'-last_seen'}.
    - TestZodSchema_RejectsInvalidStatus — `status=banana` → safeParse fails; falls back to undefined (filter "any").
    - TestZodSchema_AcceptsMultiSite — params `site=uuid1&site=uuid2` → site=[uuid1, uuid2].
    - TestZodSchema_RejectsInvalidUUID — `site=notvalid` → safeParse fails on the array element.
    - TestUseDevicesSearch_UpdatePushesHistory — `setSearch({page: 2})` → URL becomes `/devices?page=2`; back button restores `/devices?page=1`.
    - TestUseDevicesSearch_MultiValueArray — `setSearch({site: [a, b]})` → URL contains `site=a&site=b` (NOT joined).
    - TestUseDevicesSearch_ClearsFalsyParams — `setSearch({q: ''})` → URL drops the q param (clean URL).

    Tests in `index.test.tsx` (replace Wave 0 stubs):
    - TestDevicesList_FilterBySite — mock listDevices; user selects site multi-select via combobox → URL has `site=...` → useQuery re-fetches → table re-renders.
    - TestDevicesList_StatusToggleGroup — click "Active" pill → URL has `status=active` → API called with status filter.
    - TestDevicesList_LastSeenToggleGroup — click "24h" → URL has `last_seen=24h`.
    - TestDevicesList_SearchInput_Debounced — type "meter-1" → after 250ms (vi.useFakeTimers + advanceTimersByTime), URL has `q=meter-1`. Typing fast does NOT spam URL.
    - TestDevicesList_SortColumnHeader — click name header → URL has `sort=name`; second click → `sort=-name`; third click → removes sort (back to default).
    - TestDevicesList_Pagination — N=75 rows, per_page=25; click Next → page=2; "Page 2 of 3" label shown.
    - TestDevicesList_PerPageSelector — change selector to 100 → per_page=100; page resets to 1.
    - TestDevicesList_DeepLinkRestore — initial render with URL `/devices?site=uuid&status=active&last_seen=24h` → toolbar shows pre-selected filters; query fetches with these params.
  </behavior>
  <action>
    1. **`web/src/routes/devices/search-params.ts`**:

       ```typescript
       import { useCallback } from 'react'
       import { useSearchParams } from 'react-router-dom'
       import { z } from 'zod'

       export const devicesSearchSchema = z.object({
         site:      z.array(z.string().uuid()).optional().default([]),
         status:    z.enum(['active', 'inactive', 'never_joined']).optional(),
         last_seen: z.enum(['24h', '7d', '30d', 'all']).optional().default('all'),
         q:         z.string().optional().default(''),
         page:      z.coerce.number().int().min(1).default(1),
         per_page:  z.union([z.literal(25), z.literal(50), z.literal(100)]).default(50),
         sort:      z.enum(['name','-name','dev_eui','-dev_eui','site','-site','last_seen','-last_seen','created_at','-created_at']).default('-last_seen'),
       })
       export type DevicesSearch = z.infer<typeof devicesSearchSchema>

       export function useDevicesSearch(): [DevicesSearch, (next: Partial<DevicesSearch>) => void] {
         const [params, setParams] = useSearchParams()
         const raw = {
           site: params.getAll('site'),
           status: params.get('status') ?? undefined,
           last_seen: params.get('last_seen') ?? 'all',
           q: params.get('q') ?? '',
           page: params.get('page') ?? '1',
           per_page: params.get('per_page') ?? '50',
           sort: params.get('sort') ?? '-last_seen',
         }
         const parsed = devicesSearchSchema.safeParse(raw)
         const search: DevicesSearch = parsed.success ? parsed.data : devicesSearchSchema.parse({})

         const update = useCallback((next: Partial<DevicesSearch>) => {
           setParams((prev) => {
             const sp = new URLSearchParams(prev)
             for (const [k, v] of Object.entries(next)) {
               if (Array.isArray(v)) {
                 sp.delete(k); for (const item of v) sp.append(k, item)
               } else if (v === undefined || v === '' || v === null) {
                 sp.delete(k)
               } else {
                 sp.set(k, String(v))
               }
             }
             return sp
           })
         }, [setParams])

         return [search, update]
       }
       ```

    2. **`web/src/routes/devices/filter-toolbar.tsx`** — composes:
       - Site multi-select via `<MultiSelect>` (shadcn command + popover) or simple `<Select>` with multiple. Tied to `search.site` + `update({site:...})`.
       - Status `<ToggleGroup>` with options "All / Active / Inactive / Never joined".
       - Last seen `<ToggleGroup>` with options "24h / 7d / 30d / All".
       - Search `<Input>` debounced 250ms.
       - "Clear filters" ghost button — resets to defaults.

    3. **`web/src/routes/devices/index.tsx`** — REPLACE Phase 2 minimal table with TanStack Table + manualPagination + manualSorting + manualFiltering. Reads `useDevicesSearch()`; fetches `useQuery(['devices', search], () => listDevices(search))`. Column headers wrap a sort button that toggles asc/desc and updates URL. Row checkbox column (only if `user.role === 'admin'`).

       Bulk action bar (Task 2 below) integrates here.

       Add "Bulk import" + "Export current view" buttons in the toolbar; "Bulk import" launches `<BulkImportDialog>` (Task 3).
  </action>
  <acceptance_criteria>
    - `grep -q "devicesSearchSchema" web/src/routes/devices/search-params.ts` exits 0
    - `grep -q "useSearchParams" web/src/routes/devices/search-params.ts` exits 0
    - **Negative grep — NOT TanStack Router:** `grep -E "@tanstack/react-router|useSearch.*tanstack" web/src/routes/devices/search-params.ts` returns 0 lines
    - `grep -q "z.array(z.string().uuid())" web/src/routes/devices/search-params.ts` exits 0
    - `grep -q "useDevicesSearch" web/src/routes/devices/index.tsx` exits 0
    - `grep -q "manualPagination\|manualSorting" web/src/routes/devices/index.tsx` exits 0
    - `grep -q "ToggleGroup" web/src/routes/devices/filter-toolbar.tsx` exits 0
    - `grep -q "debounce\|setTimeout.*250\|useDebouncedCallback" web/src/routes/devices/filter-toolbar.tsx` exits 0
    - `cd web && pnpm test -- --run src/routes/devices/search-params src/routes/devices/index` exits 0
    - `cd web && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm test -- --run src/routes/devices/search-params src/routes/devices/index && pnpm tsc --noEmit</automated>
  </verify>
  <done>URL state hook ships with zod validation; filter toolbar + sort + pagination + deep-linking all work; tests cover happy + edge cases; NOT TanStack Router.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bulk-action bar + Bulk-decommission dialog (admin-only)</name>
  <files>web/src/routes/devices/bulk-action-bar.tsx, web/src/routes/devices/bulk-decommission-dialog.tsx, web/src/routes/devices/bulk-decommission-dialog.test.tsx, web/src/routes/devices/index.tsx</files>
  <read_first>
    - web/src/routes/gateways/decommission-gateway-dialog.tsx (Plan 03-08 — mirror the AlertDialog pattern)
    - web/src/lib/devices.ts (extend with bulkDecommissionDevices client helper)
    - .planning/phases/03-provisioning-gateways-devices-bulk-import/03-UI-SPEC.md §Bulk-action bar + §Bulk-decommission destructive dialog
  </read_first>
  <behavior>
    - TestBulkActionBar_HiddenWhenNoSelection — rowSelection={} → bar not in DOM.
    - TestBulkActionBar_ShowsCountWhenSelected — rowSelection={a:true, b:true} → bar shows "2 selected".
    - TestBulkActionBar_ClearSelection — click "Clear selection" → rowSelection={}.
    - TestBulkActionBar_ViewerHidden — viewer never sees bar (defense-in-depth).
    - TestBulkDecommissionDialog_OpenedFromBar — click Decommission button → AlertDialog opens with confirm text including "{N} devices".
    - TestBulkDecommissionDialog_ConfirmCallsAPI — confirm → bulkDecommissionDevices called with the selected ids array.
    - TestBulkDecommissionDialog_PartialSuccessToast — mock returns {succeeded:1, failed:1, outcomes:[...]} → toast.warning shows "1 succeeded, 1 failed".
    - TestBulkDecommissionDialog_AllFailuresErrorToast — mock returns {succeeded:0, failed:2} → toast.error.
  </behavior>
  <action>
    1. Extend `web/src/lib/devices.ts`:
       ```typescript
       export type BulkDecommissionResponse = {
         succeeded: number; failed: number;
         outcomes: Array<{ id: string; status: 'decommissioned' | 'failed'; reason?: string }>;
       }
       export async function bulkDecommissionDevices(deviceIds: string[], reason: string): Promise<BulkDecommissionResponse> {
         return apiFetch<BulkDecommissionResponse>('/api/devices/bulk-decommission', {
           method: 'POST', body: JSON.stringify({ device_ids: deviceIds, reason })
         })
       }
       ```

    2. **`web/src/routes/devices/bulk-action-bar.tsx`** — sticky `<div className="sticky top-... h-12 bg-secondary border-y px-4 flex items-center justify-between">`:
       ```tsx
       export function BulkActionBar({
         selectedIDs,
         onClearSelection,
         onDecommission,
       }: { selectedIDs: string[]; onClearSelection: () => void; onDecommission: () => void }) {
         if (selectedIDs.length === 0) return null
         return (
           <div className="sticky top-12 z-10 h-12 bg-secondary border-y px-4 flex items-center justify-between">
             <span className="text-sm font-semibold">{selectedIDs.length} selected</span>
             <div className="flex gap-2">
               <Button variant="destructive" size="sm" onClick={onDecommission}>
                 Decommission {selectedIDs.length} devices
               </Button>
               <Button variant="outline" size="sm">Export selected</Button>
               <Button variant="ghost" size="sm" onClick={onClearSelection}>Clear selection</Button>
             </div>
           </div>
         )
       }
       ```

    3. **`web/src/routes/devices/bulk-decommission-dialog.tsx`** — AlertDialog with body listing affected count + dialog action calling bulkDecommissionDevices then queryClient.invalidateQueries(['devices']) then toast.

    4. Wire BulkActionBar + dialog into `web/src/routes/devices/index.tsx`:
       - TanStack Table with `enableRowSelection` if admin.
       - State: `const [rowSelection, setRowSelection] = useState({})`.
       - Selected ids derived from `table.getSelectedRowModel().rows.map(r => r.original.id)`.
       - `<BulkActionBar selectedIDs={selectedIDs} ... />` rendered above the table.
       - `<BulkDecommissionDialog open={...} ... />` controlled.
  </action>
  <acceptance_criteria>
    - `grep -q "bulkDecommissionDevices" web/src/lib/devices.ts` exits 0
    - `grep -q "BulkActionBar" web/src/routes/devices/bulk-action-bar.tsx` exits 0
    - `grep -q "Decommission" web/src/routes/devices/bulk-decommission-dialog.tsx` exits 0
    - `grep -q "device_ids" web/src/routes/devices/bulk-decommission-dialog.tsx` exits 0
    - `grep -q "enableRowSelection" web/src/routes/devices/index.tsx` exits 0
    - `cd web && pnpm test -- --run src/routes/devices/bulk-decommission` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm test -- --run src/routes/devices && pnpm tsc --noEmit</automated>
  </verify>
  <done>Bulk action bar + decommission dialog wired; viewer doesn't see; partial-success outcome surfaces in toast.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bulk Import 3-step dialog + imports admin pages + sidebar nav + route registration</name>
  <files>web/src/lib/imports.ts, web/src/routes/devices/bulk-import-dialog.tsx, web/src/routes/devices/bulk-import-dialog.test.tsx, web/src/routes/admin/imports/index.tsx, web/src/routes/admin/imports/index.test.tsx, web/src/routes/admin/imports/$jobId.tsx, web/src/routes/admin/imports/$jobId.test.tsx, web/src/App.tsx, web/src/components/sidebar/nav-items.tsx</files>
  <read_first>
    - web/src/routes/devices/add-device-dialog.tsx (Plan 03-07 — stepped dialog pattern)
    - .planning/phases/03-provisioning-gateways-devices-bulk-import/03-UI-SPEC.md §Bulk Import dialog + §Imports list page + §Import job detail page
    - .planning/phases/03-provisioning-gateways-devices-bulk-import/03-CONTEXT.md D-04 / D-08 / D-09 / D-10 / D-36
    - Plan 03-05 backend contracts for /api/imports/*
  </read_first>
  <behavior>
    Bulk import dialog:
    - TestBulkImport_Step1Upload — file picker accepts .xlsx + .csv; "Download template" button GETs `/api/imports/template.xlsx`.
    - TestBulkImport_Step1Upload_NonAcceptedExtension — picking .pdf → error "supported formats: XLSX, CSV".
    - TestBulkImport_Step2Preview — after POST /api/imports returns job_id + outcomes, dialog renders banner "{valid_count} will be created, {already_exists_count} skipped, {invalid_count} errors" + TanStack Table with expandable per-row reason for invalid/already_exists rows.
    - TestBulkImport_Step2Preview_OutcomeChips — created/already_exists/skipped/error chips use semantic tokens (--success/--info/--warning/--destructive).
    - TestBulkImport_Step3Commit — button-only "Import N devices" + Cancel. No type-to-confirm.
    - TestBulkImport_Step3Commit_CallsAPI — confirm → POST /api/imports/:job_id/commit; close dialog → toast.success + queryClient.invalidateQueries(['devices']) + navigate to /admin/imports/:job_id.
    - TestBulkImport_NonUTF8CSV_RejectedWithReadableError — mock POST returns 400 "CSV is not valid UTF-8..." → dialog shows error message inline (not buried in toast).

    Imports list:
    - TestImportsList_Render — mock listImportJobs returns 3 jobs; table columns: job_id (mono, truncated), started_at, finished_at, status, totals "{created}/{skipped}/{failed}", actor.
    - TestImportsList_NewestFirst — rows ordered by created_at DESC.
    - TestImportsList_AdminOnly — viewer cannot navigate to /admin/imports (route guard); attempting → redirect home or 403 page.

    Import job detail:
    - TestImportDetail_Render — mock getImportJob returns job + 50 rows; table renders row_index, status chip, dev_eui, reason (truncated, expandable).
    - TestImportDetail_DownloadErrorsButton — shown when job has invalid_count > 0 OR failed_count > 0; click triggers `<a href="/api/imports/:job_id/errors.xlsx" download>`.
    - TestImportDetail_ExpandReason — click row chevron → reason text expanded full-width.
    - TestImportDetail_ExpiredJob — status='expired' → renders banner "This preview has expired. Re-upload to retry."
    - TestImportDetail_CommittedJob — status='committed' → summary card with counts.
  </behavior>
  <action>
    1. **`web/src/lib/imports.ts`** — API client helpers:
       ```typescript
       export type ImportJob = { id, job_id, status, file_name, file_format, total_rows, valid_count, invalid_count, already_exists_count, created_count, failed_count, expires_at, committed_at, created_at, ... }
       export type ImportJobRow = { id, row_index, status, reason, raw_payload, parsed, created_device_id }

       export async function uploadImport(file: File): Promise<{job_id: string, outcomes: ImportJobRow[], summary: ImportJob}> { ... FormData ... }
       export async function commitImport(jobID: string): Promise<ImportJob> { ... }
       export async function listImportJobs(opts: {limit?: number; offset?: number}): Promise<{total: number; items: ImportJob[]}> { ... }
       export async function getImportJob(jobID: string): Promise<{job: ImportJob; rows: ImportJobRow[]; total_rows: number}> { ... }
       export function templateDownloadURL(): string { return '/api/imports/template.xlsx' }
       export function errorsXLSXDownloadURL(jobID: string): string { return `/api/imports/${encodeURIComponent(jobID)}/errors.xlsx` }
       ```

    2. **`web/src/routes/devices/bulk-import-dialog.tsx`** — `<ResponsiveDialog sm:max-w-4xl>` with 3-step stepper. Step transitions:
       - Step 1 → Step 2 fires once upload completes (POST /api/imports returns); show inline error if upload fails.
       - Step 2 (Preview) shows summary banner (semantic-colored counts) + per-row table with row checkboxes (info only; not for selective commit — commit is all-valid-rows). Each row has chevron to expand reason.
       - Step 3 (Commit) confirms: "Import {valid_count} devices? {already_exists_count} duplicates will be skipped." Single button: "Import N devices". On click → POST commit + navigate to /admin/imports/:job_id.

    3. **`web/src/routes/admin/imports/index.tsx`** — list page with TanStack Table; clicking row → navigate to /admin/imports/:job_id.

    4. **`web/src/routes/admin/imports/$jobId.tsx`** — detail page with summary card + per-row outcomes table + Download errors.xlsx button.

    5. Add to `web/src/App.tsx`:
       ```typescript
       const AdminImportsPage = lazy(() => import('@/routes/admin/imports'))
       const ImportJobDetailPage = lazy(() => import('@/routes/admin/imports/$jobId'))
       // Inside RootLayout children:
       { path: 'admin/imports',             element: <Suspense fallback={null}><AdminImportsPage /></Suspense> },
       { path: 'admin/imports/:jobId',      element: <Suspense fallback={null}><ImportJobDetailPage /></Suspense> },
       ```

       Route guard: pages check `useCurrentUser().role === 'admin'`; non-admin redirects home.

    6. Sidebar: add "Imports" admin-only nav item under `/admin` group.
  </action>
  <acceptance_criteria>
    - `grep -q "uploadImport\|commitImport\|listImportJobs\|getImportJob" web/src/lib/imports.ts` exits 0
    - `grep -q "templateDownloadURL\|errorsXLSXDownloadURL" web/src/lib/imports.ts` exits 0
    - `grep -q "Stepper" web/src/routes/devices/bulk-import-dialog.tsx` exits 0
    - `grep -q "Download template" web/src/routes/devices/bulk-import-dialog.tsx` exits 0
    - `grep -q "Import .* devices" web/src/routes/devices/bulk-import-dialog.tsx` exits 0
    - `grep -q "Download errors.xlsx\|errorsXLSXDownloadURL" web/src/routes/admin/imports/\$jobId.tsx` exits 0
    - `grep -q "expired\|This preview has expired" web/src/routes/admin/imports/\$jobId.tsx` exits 0
    - `grep -q "AdminImportsPage\|/admin/imports" web/src/App.tsx` exits 0
    - **Negative grep — UX-03:** `grep -iE "(tenant|application)" web/src/routes/admin/imports/*.tsx web/src/routes/devices/bulk-import-dialog.tsx | grep -v -E "(applicationId|applicationLogic|application/vnd)"` returns 0 user-facing matches
    - `cd web && pnpm test -- --run src/routes/devices/bulk-import src/routes/admin/imports` exits 0
    - `cd web && pnpm tsc --noEmit && pnpm build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm test -- --run src/routes/devices src/routes/admin/imports && pnpm tsc --noEmit && pnpm build</automated>
  </verify>
  <done>Bulk import dialog 3-step flow ships; imports admin list + detail pages live; sidebar entry; route registered; admin guard enforced (defense-in-depth — server is authoritative).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /api/imports (file) | Multipart upload; size cap 5MB enforced server-side (Plan 03-05); UI hints at limit. |
| browser → POST /api/devices/bulk-decommission | Admin-only server-side; UI hides for viewer. |
| browser → /admin/imports route | UI redirects non-admins; server still enforces ActionDeviceBulkImport on every endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verified By |
|-----------|----------|-----------|----------|-------------|-----------------|-------------|
| T-3-90 | E (Elevation of Privilege) | viewer accesses /admin/imports via direct URL | medium | mitigate | Route guard redirects non-admin; server-side RequireAction still primary defense; Plan 03-05 endpoints all admin-gated. | Task 3 TestImportsList_AdminOnly |
| T-3-91 | T (Tampering) | client editing URL to inject invalid filter params | low | mitigate | zod safeParse falls back to defaults on parse failure (silent recovery); server validates again. | Task 1 TestZodSchema_RejectsInvalidStatus |
| T-3-92 | D (DoS) | client uploads 5GB file | **high** | mitigate | http.MaxBytesReader at backend (Plan 03-05); browser also rejects via `<input type="file" accept=".xlsx,.csv">` + post-pick size check (5MB) showing error before upload. | Task 3 |
| T-3-93 | I (Information Disclosure) | imports list reveals other users' imports | low | accept | Single-tenant install — all admins legitimately see all imports. Audit log distinguishes actor. | accept |
| T-3-94 | T (Tampering) | filter param `q` containing SQL injection attempt | low | mitigate | sqlc parameterised query (Plan 03-06); URL state hook does NOT do string interp. | Phase 2 baseline |
| T-3-95 | I (Information Disclosure) | UX-03 vocabulary leak in admin/imports or bulk-import dialog | low | mitigate | Negative grep + vitest render assertion. | Task 3 |
</threat_model>

<verification>
- `cd web && pnpm test -- --run && pnpm tsc --noEmit && pnpm build` exits 0
- URL state implementation uses ONLY react-router-dom v7 useSearchParams (not TanStack Router)
- Bulk import 3-step flow + idempotency surfacing (already_exists outcomes visible to operator) works end-to-end against mocked backend
- UX-03 negative grep passes across all 8 new files
- Sidebar shows Imports under /admin group (admin-only)
</verification>

<success_criteria>
- DEV-01 frontend: filter/sort/pagination/URL-state complete
- DEV-06 frontend: 3-step bulk import dialog with dry-run preview → commit confirm
- DEV-07 visibility: re-uploaded same file shows `already_exists` outcome chips (--info, NOT --warning)
- DEV-08: "Download template" button live + template downloads cleanly
- D-36: /admin/imports/:jobId detail page with row outcomes + Download errors.xlsx
- UX-03 maintained across all new surfaces
- Subsequent Plan 03-10 reveal keys dialog + vocabulary audit + REQUIREMENTS reconciliation
</success_criteria>

<output>
After completion, create `.planning/phases/03-provisioning-gateways-devices-bulk-import/03-09-SUMMARY.md` documenting:
- URL state contract: exact zod schema + query param naming conventions
- "NOT TanStack Router" correction note — react-router-dom v7 useSearchParams is the canonical choice for this project
- Bulk import 3-step UX flow with screenshots-equivalent prose
- /admin/imports route registration + admin guard
- Outcome chip → semantic token mapping (created→success, already_exists→info, skipped/invalid→warning, error→destructive)
- UX-03 negative grep results across all 8 new files
</output>
