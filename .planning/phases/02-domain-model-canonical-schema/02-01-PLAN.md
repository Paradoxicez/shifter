---
phase: 02-domain-model-canonical-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/pnpm-lock.yaml
  - internal/site/handlers_test.go
  - internal/meteringpoint/handlers_test.go
  - internal/device/handlers_test.go
  - internal/device/deveui_test.go
  - internal/swap/math_test.go
  - internal/swap/commit_test.go
  - internal/ingest/handler_test.go
  - internal/ingest/normalize_test.go
  - internal/resolver/cache_test.go
  - internal/resolver/listener_test.go
  - internal/profile/editor_test.go
  - internal/profile/seed_test.go
  - internal/audit/log_test.go
  - internal/audit/diff_test.go
  - internal/testharness/scenarios_test.go
  - internal/chirpstack/tenant_test.go
  - internal/chirpstack/application_test.go
  - internal/chirpstack/device_profile_test.go
  - internal/chirpstack/device_test.go
  - internal/chirpstack/bootstrap_test.go
  - internal/testsupport/mosquitto.go
  - web/src/routes/sites/create-site-dialog.test.tsx
  - web/src/routes/devices/add-device-dialog.test.tsx
  - web/src/routes/devices/deveui-parser.test.tsx
  - web/src/routes/metering-points/swap-meter-dialog.test.tsx
  - web/src/routes/profiles/mapping-editor.test.tsx
  - .planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md
autonomous: true
requirements: [SITE-01, DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06, DATA-07, DATA-08, DATA-09, DATA-10, AUDIT-01, CHIRP-04]

must_haves:
  truths:
    - "Every Phase 2 source file has a paired *_test.go (or *.test.tsx) file with t.Skip() / it.skip()."
    - "@tanstack/react-table is installed in web/package.json and resolvable in web/node_modules."
    - "An eclipse-mosquitto:2 testcontainer helper is callable from Go integration tests."
    - "02-VALIDATION.md is fully populated (no template placeholders) and nyquist_compliant: true."
  artifacts:
    - path: "internal/site/handlers_test.go"
      provides: "Skipped scaffold test for SITE-01 site CRUD"
      contains: "t.Skip"
    - path: "internal/swap/math_test.go"
      provides: "Skipped scaffold for ProposeOffset + DetectRollover"
      contains: "t.Skip"
    - path: "internal/testsupport/mosquitto.go"
      provides: "StartMosquitto helper for integration tests"
      exports: ["StartMosquitto"]
    - path: "web/package.json"
      provides: "@tanstack/react-table dependency"
      contains: "@tanstack/react-table"
    - path: ".planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md"
      provides: "Per-task verification map populated for every Phase 2 plan"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "internal/swap/math_test.go"
      to: "internal/swap/math.go (created in Plan 07)"
      via: "package import"
      pattern: "package swap"
    - from: "internal/testsupport/mosquitto.go"
      to: "internal/testharness/scenarios_test.go"
      via: "MQTT testcontainer broker URL"
      pattern: "StartMosquitto"
---

<objective>
Wave 0 scaffolding for Phase 2: install @tanstack/react-table, add a Mosquitto testcontainer helper, and create skipped-stub test files for every Phase 2 production source file. Fill out 02-VALIDATION.md so Nyquist sampling is enforceable from Plan 02 onwards.

Purpose: Phase 1 locked the canonical pattern that every <verify> block points at an existing test file (`t.Skip` / `it.skip`); the implementing plan replaces only the body. Phase 2 inherits this rule. No subsequent Phase 2 plan may create a test file from scratch — it must edit one of the stubs created here. (See Plan 01-02 SUMMARY: "Future plans MUST NOT create new test files outside the scaffold from this plan.")

Output: 21 backend test stubs, 5 frontend test stubs, 1 testcontainer helper, 1 frontend dependency added, 1 fully populated VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-domain-model-canonical-schema/02-CONTEXT.md
@.planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md
@.planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@web/package.json
@internal/testsupport/postgres.go
</context>

<interfaces>
<!-- Pattern from Phase 1 — every test stub follows this exact shape so the implementing plan only changes the body. -->

Go skip stub (Phase 1 canonical):
```go
package swap

import "testing"

func TestProposeOffset(t *testing.T) {
    t.Skip("TODO(plan-02-07): implement after internal/swap/math.go exists")
}
```

Frontend skip stub:
```tsx
import { describe, it } from "vitest";

describe("CreateSiteDialog", () => {
  it.skip("TODO(plan-02-12): implement after create-site-dialog.tsx exists", () => {});
});
```

Mosquitto testcontainer helper (extends Phase 1 testsupport pattern):
```go
package testsupport

import (
    "context"
    "testing"

    "github.com/testcontainers/testcontainers-go"
    "github.com/testcontainers/testcontainers-go/wait"
)

// StartMosquitto returns a broker URL like tcp://127.0.0.1:51234 backed by
// an ephemeral eclipse-mosquitto:2.0.20 container. Same pattern as Phase 1's
// StartPostgres helper. Pin the tag — never :latest (T-02-01 mitigation).
func StartMosquitto(t *testing.T) string { ... }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install @tanstack/react-table and add Mosquitto testcontainer helper</name>
  <files>web/package.json, web/pnpm-lock.yaml, internal/testsupport/mosquitto.go</files>
  <read_first>
    - web/package.json (verify current dependency tree before adding)
    - internal/testsupport/postgres.go (mirror its constructor shape for StartMosquitto)
    - .planning/phases/01-foundation/01-02-SUMMARY.md (Plan 01-02 — pinned testcontainer tag rule: T-02-01 mitigation; no `:latest`)
  </read_first>
  <action>
    Step A — install frontend dependency:
    `cd web && pnpm add @tanstack/react-table` (do not pin major; let pnpm resolve current v8.x). Confirm the entry appears under "dependencies" (not devDependencies) — runtime feature.

    Step B — write `internal/testsupport/mosquitto.go`:
    Function signature `func StartMosquitto(t *testing.T) string`. Implementation uses `testcontainers.GenericContainer` with `eclipse-mosquitto:2.0.20` (PIN this tag exactly — never `:latest`, per T-02-01 from Plan 01-02). Container exposes port 1883/tcp. Mount an inline allow-anonymous mosquitto config via `ContainerFile{Reader: strings.NewReader("listener 1883\\nallow_anonymous true\\n"), HostFilePath: "", ContainerFilePath: "/mosquitto/config/mosquitto.conf", FileMode: 0o644}`. Wait strategy: `wait.ForListeningPort("1883/tcp")` with 30s timeout. Register `t.Cleanup(func() { _ = container.Terminate(context.Background()) })`. Return `tcp://<host>:<mapped_port>` URL via `container.Endpoint(ctx, "tcp")`.

    Use the SAME error handling style as `StartPostgres` (call `t.Fatalf` on container start failure, never panic). Add a package doc comment "// StartMosquitto returns a broker URL backed by an ephemeral eclipse-mosquitto:2.0.20 container. Auto-cleaned via t.Cleanup. Used by internal/testharness and integration tests in internal/ingest, internal/resolver, internal/profile."

    Run `go build ./internal/testsupport/...` to confirm compilation.
  </action>
  <verify>
    <automated>cd web && pnpm list @tanstack/react-table | grep -q "@tanstack/react-table" &amp;&amp; cd .. &amp;&amp; go build ./internal/testsupport/...</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm list @tanstack/react-table` lists a v8.x version (not "(missing)").
    - `web/package.json` `dependencies` block contains `"@tanstack/react-table"`.
    - `internal/testsupport/mosquitto.go` exists and contains `func StartMosquitto(t *testing.T) string`.
    - `internal/testsupport/mosquitto.go` contains the literal `eclipse-mosquitto:2.0.20` (NOT `:latest`).
    - `internal/testsupport/mosquitto.go` contains `t.Cleanup(`.
    - `go build ./internal/testsupport/...` exits 0.
  </acceptance_criteria>
  <done>@tanstack/react-table installed and StartMosquitto compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create 21 backend test stubs + 5 frontend test stubs</name>
  <files>internal/site/handlers_test.go, internal/meteringpoint/handlers_test.go, internal/device/handlers_test.go, internal/device/deveui_test.go, internal/swap/math_test.go, internal/swap/commit_test.go, internal/ingest/handler_test.go, internal/ingest/normalize_test.go, internal/resolver/cache_test.go, internal/resolver/listener_test.go, internal/profile/editor_test.go, internal/profile/seed_test.go, internal/audit/log_test.go, internal/audit/diff_test.go, internal/testharness/scenarios_test.go, internal/chirpstack/tenant_test.go, internal/chirpstack/application_test.go, internal/chirpstack/device_profile_test.go, internal/chirpstack/device_test.go, internal/chirpstack/bootstrap_test.go, web/src/routes/sites/create-site-dialog.test.tsx, web/src/routes/devices/add-device-dialog.test.tsx, web/src/routes/devices/deveui-parser.test.tsx, web/src/routes/metering-points/swap-meter-dialog.test.tsx, web/src/routes/profiles/mapping-editor.test.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-02-SUMMARY.md (canonical Wave 0 stub-then-fill pattern — Section "Plan 01-02 — Wave 0 stub-then-fill is the canonical pattern")
    - internal/auth/can.go (existing test pattern; mirror file-level layout)
    - .planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md §"Wave 0 Gaps" (the authoritative list of 26 stub files)
  </read_first>
  <action>
    Create 26 test stub files. For EACH backend file create a Go file with this exact body (substituting package name and TODO plan number):

    ```go
    package <pkgname>

    import "testing"

    // TODO(plan-02-NN): implement after <relevant-source-file>.go exists.
    // Phase 1 D-02 + Plan 01-02 SUMMARY locks Wave 0 stub-then-fill: this file
    // exists so future plans EDIT it rather than CREATE a new test file.

    func TestPlaceholder(t *testing.T) {
        t.Skip("Wave 0 placeholder — see TODO above")
    }
    ```

    Plan number mapping (used in TODO marker — must match the implementing plan exactly):
    - internal/site/handlers_test.go → plan-02-10
    - internal/meteringpoint/handlers_test.go → plan-02-10
    - internal/device/handlers_test.go → plan-02-10
    - internal/device/deveui_test.go → plan-02-10
    - internal/swap/math_test.go → plan-02-07
    - internal/swap/commit_test.go → plan-02-07
    - internal/ingest/handler_test.go → plan-02-09
    - internal/ingest/normalize_test.go → plan-02-09
    - internal/resolver/cache_test.go → plan-02-07
    - internal/resolver/listener_test.go → plan-02-07
    - internal/profile/editor_test.go → plan-02-08
    - internal/profile/seed_test.go → plan-02-08
    - internal/audit/log_test.go → plan-02-07
    - internal/audit/diff_test.go → plan-02-07
    - internal/testharness/scenarios_test.go → plan-02-15
    - internal/chirpstack/tenant_test.go → plan-02-05
    - internal/chirpstack/application_test.go → plan-02-05
    - internal/chirpstack/device_profile_test.go → plan-02-05
    - internal/chirpstack/device_test.go → plan-02-05
    - internal/chirpstack/bootstrap_test.go → plan-02-05

    Package names: `site`, `meteringpoint`, `device`, `swap`, `ingest`, `resolver`, `profile`, `audit`, `testharness`, `chirpstack` (the chirpstack tests live in the existing internal/chirpstack/ package — verify package line matches `package chirpstack`).

    Create the parent directories with `mkdir -p internal/{site,meteringpoint,device,swap,ingest,resolver,profile,audit,testharness}` and `mkdir -p web/src/routes/{sites,devices,metering-points,profiles}` BEFORE writing files.

    Each backend package also needs a 2-line `doc.go`:
    ```go
    // Package <name> — Phase 2 placeholder. See plan-02-NN for body.
    package <name>
    ```

    Frontend stubs (5 files): each is a `*.test.tsx` with this body:
    ```tsx
    import { describe, it } from "vitest";

    describe("<ComponentName>", () => {
      it.skip("TODO(plan-02-NN): implement after <component-file>.tsx exists", () => {});
    });
    ```
    Frontend plan numbers:
    - web/src/routes/sites/create-site-dialog.test.tsx → plan-02-12
    - web/src/routes/devices/add-device-dialog.test.tsx → plan-02-13
    - web/src/routes/devices/deveui-parser.test.tsx → plan-02-13
    - web/src/routes/metering-points/swap-meter-dialog.test.tsx → plan-02-14
    - web/src/routes/profiles/mapping-editor.test.tsx → plan-02-14

    After writing, run `go test -count=1 -run TestPlaceholder ./internal/...` and `cd web && pnpm test:run` — both MUST exit 0 with skipped tests reported.
  </action>
  <verify>
    <automated>go test -count=1 -run TestPlaceholder ./internal/site/... ./internal/meteringpoint/... ./internal/device/... ./internal/swap/... ./internal/ingest/... ./internal/resolver/... ./internal/profile/... ./internal/audit/... ./internal/testharness/... ./internal/chirpstack/... &amp;&amp; cd web &amp;&amp; pnpm test:run</automated>
  </verify>
  <acceptance_criteria>
    - All 21 backend test files exist (verify with `ls internal/{site,meteringpoint,device,swap,ingest,resolver,profile,audit,testharness}/*_test.go internal/chirpstack/{tenant,application,device_profile,device,bootstrap}_test.go`).
    - All 5 frontend test files exist (verify with `ls web/src/routes/{sites/create-site-dialog,devices/add-device-dialog,devices/deveui-parser,metering-points/swap-meter-dialog,profiles/mapping-editor}.test.tsx`).
    - Every backend test file contains the literal string `t.Skip(`.
    - Every frontend test file contains `it.skip(`.
    - Every backend test file's TODO marker matches the implementing plan number from the table above (grep `TODO(plan-02-` in each file MUST find exactly one match).
    - Each new backend package directory has a `doc.go` file (verify `ls internal/{site,meteringpoint,device,swap,ingest,resolver,profile,audit,testharness}/doc.go`).
    - `go test -count=1 ./internal/...` exits 0 (skipped tests count as pass).
    - `cd web && pnpm test:run` exits 0.
  </acceptance_criteria>
  <done>26 stub files exist, all skip cleanly, future plans edit them in place.</done>
</task>

<task type="auto">
  <name>Task 3: Populate 02-VALIDATION.md with full per-task verification map</name>
  <files>.planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md (current template skeleton)
    - .planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md §"Validation Architecture" (test framework + per-req map + sampling rate)
    - .planning/phases/01-foundation/01-VALIDATION.md (canonical example to mirror)
  </read_first>
  <action>
    Replace the template body of 02-VALIDATION.md with a fully populated contract using the exact data from RESEARCH §Validation Architecture. Set frontmatter `nyquist_compliant: true` and `wave_0_complete: true` (because Task 2 just created all the stubs).

    Sections to fill out:

    1. Test Infrastructure table:
       - Backend framework: `Go testing + testify v1.11.1 + testcontainers-go v0.42.0`
       - Backend config: `none (Go convention)`
       - Frontend framework: `vitest 4.x + @testing-library/react 16.x`
       - Frontend config: `web/vite.config.ts`
       - Quick run command: `go test ./internal/<changed>/... -race -short && cd web && pnpm test:run -- <relevant>`
       - Full suite command: `go test -tags=integration -race -count=1 ./... && cd web && pnpm test:run && pnpm build`
       - Estimated runtime: ~150 seconds (includes Mosquitto + Postgres+TS testcontainers cold start)

    2. Sampling Rate:
       - After every task commit: quick run command above
       - After every plan wave: full suite command above
       - Before /gsd-verify-work: full suite + 10 testharness scenarios all green
       - Max feedback latency: 30 seconds

    3. Per-Task Verification Map — ONE ROW per requirement (13 reqs) with these columns: `Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status`. Use the data from RESEARCH §"Phase Requirements → Test Map" table verbatim. Plan/Wave/Task IDs use the assignments from this plan set (e.g. SITE-01 → 02-10-01, Wave 4). Threat Ref pulls from RESEARCH §Security Domain (e.g. AUDIT-01 → "Pattern 8 atomic audit txn"). File Exists column = ✅ W0 (since Task 2 created stubs). Status column = ⬜ pending.

    4. Wave 0 Requirements section: list all 26 stub files Task 2 created with `[x]` checkmarks (since they exist after Task 2). Add the line "Mosquitto testcontainer helper at internal/testsupport/mosquitto.go — created Plan 02-01 Task 1." Add `pnpm add @tanstack/react-table` line — done in Task 1.

    5. Manual-Only Verifications: list "All phase behaviors have automated verification." (None of Phase 2's REQs are manual-only.)

    6. Validation Sign-Off checkboxes: check ALL 6 (Wave 0 complete, no MISSING refs, no watch-mode flags, feedback < 30s).

    7. Set "Approval: pending — to be marked approved when execute-phase wraps."

    Frontmatter must contain (verbatim):
    ```yaml
    nyquist_compliant: true
    wave_0_complete: true
    ```
  </action>
  <verify>
    <automated>grep -q 'nyquist_compliant: true' .planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md &amp;&amp; grep -q 'wave_0_complete: true' .planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md &amp;&amp; ! grep -q '{N}' .planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md &amp;&amp; ! grep -q '{quick command}' .planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `02-VALIDATION.md` frontmatter contains `nyquist_compliant: true` AND `wave_0_complete: true`.
    - File contains NO template placeholders: `grep -E '\{N\}|\{quick command\}|\{full command\}|\{framework\}|REQ-\{XX\}'` returns 0 matches.
    - Per-task map has at least 13 rows (one per requirement: SITE-01, DATA-01..10, AUDIT-01, CHIRP-04).
    - Per-task map row for SITE-01 contains automated command `go test ./internal/site/...`.
    - Per-task map row for DATA-06 contains automated command `go test -tags=integration ./internal/testharness/...`.
    - Per-task map row for CHIRP-04 contains automated command referencing `bufconn` mock OR `internal/device/...`.
    - Wave 0 Requirements section lists all 26 stub files with `[x]`.
    - All 6 Validation Sign-Off checkboxes are `[x]`.
  </acceptance_criteria>
  <done>02-VALIDATION.md is the binding sampling contract; nyquist_compliant flag flips to true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test container ↔ host | testcontainers spin up arbitrary images; pinning the eclipse-mosquitto tag prevents supply-chain drift |
| Frontend npm registry ↔ build | `pnpm add @tanstack/react-table` pulls from the public npm registry; pnpm-lock.yaml integrity hash gates supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | T (Tampering) | testcontainer image tag | mitigate | PIN `eclipse-mosquitto:2.0.20` (never `:latest`); identical to Plan 01-02's T-02-01 mitigation. Acceptance criterion checks for the literal pinned tag. |
| T-02-01-02 | T (Tampering) | npm dependency | mitigate | `pnpm-lock.yaml` is committed; pnpm verifies integrity hashes on install. Re-add if a lock-file mismatch is detected. |
| T-02-01-03 | I (Information Disclosure) | placeholder test files | accept | Stubs contain only `t.Skip` and `TODO(plan-...)` markers — no secrets, no PII, no production logic. |
</threat_model>

<verification>
After all 3 tasks:
- `go test -count=1 ./internal/...` exits 0 (all stubs skipped, no compile errors).
- `cd web && pnpm test:run` exits 0 (all 5 stubs skipped).
- `cd web && pnpm typecheck` (if available, otherwise `pnpm build`) confirms TanStack Table types resolve.
- `go build ./internal/testsupport/...` exits 0.
- `02-VALIDATION.md` is no longer a template — every `{...}` placeholder is filled in.
</verification>

<success_criteria>
1. 26 stub test files exist (21 backend + 5 frontend) and all skip-pass cleanly.
2. Mosquitto testcontainer helper compiles and pins `eclipse-mosquitto:2.0.20`.
3. `@tanstack/react-table` is installed in `web/package.json`.
4. `02-VALIDATION.md` is fully populated and `nyquist_compliant: true`.
5. Every Phase 2 plan now has a stub file it can edit in place — no new test files will be created downstream.
</success_criteria>

<output>
After completion, create `.planning/phases/02-domain-model-canonical-schema/02-01-SUMMARY.md`.
</output>
