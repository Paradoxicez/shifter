---
phase: 02-domain-model-canonical-schema
plan: 15
type: execute
gap_closure: true
wave: 10
depends_on: [02-11, 02-12, 02-13, 02-14]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md
  - .planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SITE-01, DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06, DATA-07, DATA-08, DATA-09, DATA-10, AUDIT-01, CHIRP-04]

must_haves:
  truths:
    - "REQUIREMENTS.md status column reflects post-gap-closure reality: every Phase 2 requirement is correctly marked Complete, Backend-Only (rare — only if a deferred UI piece exists), or Pending; no requirement is overstated."
    - "VALIDATION.md per-task verification map has every ⬜ pending row resolved: each becomes ✅ green pointing at a concrete test command (Plans 02-11..14 outputs) OR ⚠ flaky with documented reason OR is downgraded to a Manual-Only item if no automated proof is reachable."
    - "RESEARCH.md `## Open Questions` heading flipped to `## Open Questions (RESOLVED)` with all 5 questions inline-marked `RESOLVED: <decision>` and a citation to the landed implementation. Phase-wide Dimension 11 hygiene blocker closed."
    - "STATE.md reflects that Phase 2 gap closure has executed; if Phase 2 is complete it gets marked Done, otherwise the remaining gaps are documented."
    - "ROADMAP.md Progress table updates Phase 2 from '6/10 In Progress' to '15/15 Complete' (or whatever count includes the 5 gap-closure plans)."
    - "Final test suite run is clean: go test ./... -race -count=1 + pnpm --dir web test --run + pnpm --dir web build + just lint all exit 0."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability table with corrected statuses for SITE-01, DATA-01..05, DATA-07..10, AUDIT-01, CHIRP-04 (was prematurely marked Complete; this plan reconciles to actual state)."
    - path: ".planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md"
      provides: "Per-task map flipped from all-pending to all-resolved; cross-reference to TestScenario_* (DATA-06), TestScenario_AxiomaW1_E2E (DATA-10), all the dialog tests (SITE-01, DATA-04, DATA-09, CHIRP-04 frontend half), TestServe_FullBoot_* (DATA-03/07 runtime wiring half)."
    - path: ".planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md"
      provides: "Open Questions block flipped to (RESOLVED) with inline citations to landed implementations. Dimension 11 hygiene gap closed."
    - path: ".planning/STATE.md"
      provides: "Phase 2 marked complete with gap-closure history footnote; next-phase pointer updated to Phase 3 if applicable."
    - path: ".planning/ROADMAP.md"
      provides: "Progress table updated; Phase 2 plan count updated from 10 to 15."
  key_links:
    - from: ".planning/REQUIREMENTS.md DATA-06 row"
      to: "internal/testharness/scenarios_test.go::TestScenario_*"
      via: "status: Pending → Complete; evidence column references the 5 named tests"
      pattern: "DATA-06 .* Phase 2 .* Complete"
    - from: ".planning/REQUIREMENTS.md SITE-01 row"
      to: "web/src/routes/sites/create-site-dialog.test.tsx + internal/site/handlers_test.go"
      via: "status: Backend-Only → Complete (frontend dialog now ships)"
      pattern: "SITE-01"
    - from: ".planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md ⬜ rows"
      to: "concrete test commands from Plans 02-11..14"
      via: "each row's Status column flips ⬜ → ✅ + Automated Command column populated"
      pattern: "✅ green"
    - from: ".planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md ## Open Questions"
      to: "Plan 02-03 / 02-07 / 02-08 / 02-09 SUMMARY landed implementations"
      via: "each Q1..Q5 inline-marked RESOLVED with citation"
      pattern: "## Open Questions \\(RESOLVED\\)"
---

<objective>
This plan is documentation-only. After Plans 02-11..14 land their code + tests, REQUIREMENTS.md / VALIDATION.md / RESEARCH.md / STATE.md / ROADMAP.md need to reflect the new reality. The verifier flagged that REQUIREMENTS.md was overstating completion (marking SITE-01 / DATA-04 / CHIRP-04 etc. "Complete" when only the backend half had landed). After gap closure those statuses ARE accurate, but only if this plan flips them deliberately + audits them. Additionally, the plan-checker (gap-closure revision pass) flagged RESEARCH.md's `## Open Questions` block (lines 945-971) as never resolved despite the actual decisions having landed in Plans 02-03 / 02-07 / 02-08 / 02-09 — Task 4 closes that hygiene gap.

Purpose: Closes the meta-gap from VERIFICATION.md "Note for REQUIREMENTS.md hygiene" + the VALIDATION.md ⬜ pending rows + RESEARCH.md Dimension 11 Open Questions block. After this plan, the planning docs match the code — a future verifier (or human reviewer) will not see drift between "what we say is done" and "what is actually done."

Output: Edits to 5 markdown files in .planning/. No code changes. Wave 10: last in the chain because every other plan must have actually executed + produced its evidence trail before this plan can responsibly mark statuses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md
@.planning/phases/02-domain-model-canonical-schema/02-VERIFICATION.md
@.planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md
@.planning/phases/02-domain-model-canonical-schema/02-CONTEXT.md
@.planning/phases/02-domain-model-canonical-schema/02-03-SUMMARY.md
@.planning/phases/02-domain-model-canonical-schema/02-07-SUMMARY.md
@.planning/phases/02-domain-model-canonical-schema/02-08-SUMMARY.md
@.planning/phases/02-domain-model-canonical-schema/02-09-SUMMARY.md
@.planning/phases/02-domain-model-canonical-schema/02-11-SUMMARY.md
@.planning/phases/02-domain-model-canonical-schema/02-12-SUMMARY.md
@.planning/phases/02-domain-model-canonical-schema/02-13-SUMMARY.md
@.planning/phases/02-domain-model-canonical-schema/02-14-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full Phase 2 test suite + capture evidence</name>
  <read_first>
    - .planning/phases/02-domain-model-canonical-schema/02-VERIFICATION.md (the bar to clear: every gap's `missing` array now has shipping evidence)
    - All 4 prior plan SUMMARYs (02-11..14) — they document each plan's tests + acceptance
  </read_first>
  <files>(no files modified — this task gathers evidence)</files>
  <action>
    Run the full suite and capture results in a temporary scratch buffer (or task-output snippet) for Tasks 2 + 3 + 4 to consume:

    1. **Go test suite**:
       ```bash
       go test ./... -race -count=1 -timeout 300s 2>&1 | tee /tmp/02-15-go-test.log
       ```
       Record: total tests, pass count, any FAIL or SKIP lines (excluding the deliberate `-short` skips on integration tests). Expected: 0 fails. The Plan 02-09 baseline was 242 short tests; with the 5 gap-closure plans the count should be ~310-340 short tests + the ~30 integration tests added by 02-12 + 02-13.

    2. **Frontend test suite**:
       ```bash
       pnpm --dir web test --run 2>&1 | tee /tmp/02-15-pnpm-test.log
       ```
       Record: total, pass, skip count. Expected: 0 skips on Phase 2 dialog test files (the 5 originally-stubbed files are all flipped). Phase 1 component tests are unchanged.

    3. **Build**:
       ```bash
       pnpm --dir web build 2>&1 | tail -20 | tee /tmp/02-15-build.log
       ```
       Expected: clean dist/ output; no TS errors.

    4. **Lint**:
       ```bash
       just lint 2>&1 | tee /tmp/02-15-lint.log
       ```
       Or whatever the project's lint entry is (the executor reads Justfile to confirm). Expected: 0 errors.

    5. **Schema integrity grep**:
       ```bash
       grep -iE 'dev_eui|device_id' internal/db/migrations/0015_measurement.up.sql 2>&1
       ```
       Confirms DATA-01 invariant still holds (no dev_eui or device_id column on measurement). Expected: 0 lines in column-definition contexts.

    6. **DATA-06 evidence (per-test loop, not single grep — W4 from gap-closure revision)**:
       ```bash
       go test ./internal/testharness/... -run "TestScenario_" -count=1 -timeout 300s 2>&1 | tee /tmp/02-15-testharness.log
       ```
       Then verify EACH named scenario passed individually (handles "5 of 6 passed" silent regressions that a single count-grep would miss):
       ```bash
       for t in CleanSwap SwapWithInflightUplink Rollover SwapAndRollover OverlappingUplinks AxiomaW1_E2E; do
         grep -F "--- PASS: TestScenario_$t" /tmp/02-15-testharness.log || echo "MISSING: TestScenario_$t"
       done > /tmp/02-15-testharness-results.txt
       test $(grep -c "^MISSING:" /tmp/02-15-testharness-results.txt) -eq 0
       ```
       Expected: zero MISSING lines. If any scenario is missing, the loop's `test` exits non-zero and Task 2 must NOT flip DATA-06 to Complete; instead the missing list is embedded into 02-15-SUMMARY.md and an issue raised.

    7. **Boot wiring evidence**:
       ```bash
       go test ./internal/cli/... -run "TestServe_FullBoot" -count=1 -timeout 240s 2>&1 | tee /tmp/02-15-serve.log
       ```
       Expected: 4/4 PASS.

    8. **REQUIREMENTS.md drift check** — for each Phase 2 requirement (SITE-01, DATA-01..10, AUDIT-01, CHIRP-04), grep for evidence:
       - SITE-01: site/handlers_test.go has TestCreateSite_* PASS + sites/create-site-dialog.test.tsx has 4 PASS
       - DATA-01..05/07/08: existing 02-04..09 tests + ingest pipeline live (TestServe_FullBoot_MQTTUplinkPersists confirms wired)
       - DATA-06: TestScenario_* 5 + 1 PASS (per-test loop above)
       - DATA-09: profile/handlers_test.go has TestProfileHandler_* PASS + mapping-editor.test.tsx has 6 PASS
       - DATA-10: TestScenario_AxiomaW1_E2E PASS
       - AUDIT-01: existing 13 same-tx call sites still work; verifier already confirmed
       - CHIRP-04: device/handlers_test.go has TestAddDevice_* PASS + add-device-dialog.test.tsx has 3 PASS + serve_test.go boots routes

    Compile this evidence into a markdown table for Tasks 2 + 3 + 4 to embed.
  </action>
  <verify>
    <automated>echo "evidence captured in /tmp/02-15-*.log"; ls -la /tmp/02-15-*.log /tmp/02-15-testharness-results.txt</automated>
  </verify>
  <acceptance_criteria>
    - All 4 logs present in /tmp: `ls /tmp/02-15-*.log` returns at least 6 files
    - go test exit 0: `tail -1 /tmp/02-15-go-test.log` does NOT contain "FAIL"
    - pnpm test exit 0: `tail -1 /tmp/02-15-pnpm-test.log` shows pass count > 0 + 0 fails
    - pnpm build exit 0: `tail -1 /tmp/02-15-build.log` does NOT contain "error"
    - testharness 6/6 (per-test loop, W4): `test $(grep -c "^MISSING:" /tmp/02-15-testharness-results.txt) -eq 0` exits 0; the file lists each TestScenario_$t found, no MISSING lines
    - serve_test 4/4: `grep -c "^--- PASS: TestServe_FullBoot" /tmp/02-15-serve.log` returns 4
    - Schema invariant intact: `grep -iE 'dev_eui|device_id' internal/db/migrations/0015_measurement.up.sql | grep -v -- '--'` returns 0 lines
  </acceptance_criteria>
  <done>Evidence trail captured for every Phase 2 requirement. Tasks 2 + 3 + 4 can flip statuses with confidence.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md + VALIDATION.md statuses</name>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — focus on Traceability table lines 226-326)
    - .planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md (per-task map lines 46-86)
    - Task 1 evidence logs (/tmp/02-15-*.log + /tmp/02-15-testharness-results.txt)
    - .planning/phases/02-domain-model-canonical-schema/02-VERIFICATION.md "Note for REQUIREMENTS.md hygiene" (lines 238-242 of the verification report — explicit list of overstatements)
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md</files>
  <action>
    1. **Edit .planning/REQUIREMENTS.md Traceability table** (the row-per-requirement section starting at line ~226):

       Update each Phase 2 row's Status column based on Task 1 evidence:
       - **SITE-01** Phase 2 — was "Complete" → **stays Complete** (frontend dialog + backend handlers + tests all green)
       - **DATA-01** Phase 2 — was "Complete" → **stays Complete** (schema invariant + resolver + ingest pipeline wired + tests green)
       - **DATA-02** Phase 2 — was "Complete" → **stays Complete**
       - **DATA-03** Phase 2 — was "Complete" → **stays Complete** (ingest captures server time + TestServe_FullBoot proves runtime wiring)
       - **DATA-04** Phase 2 — was "Complete" → **stays Complete** (swap math + commit + handler + dialog all green)
       - **DATA-05** Phase 2 — was "Complete" → **stays Complete** (rollover detection + audit + serve.go wires ingest pipeline)
       - **DATA-06** Phase 2 — was "Pending" → **flip to Complete** (5 scenarios + AxiomaW1 E2E all PASS per Task 1 W4 per-test loop)
       - **DATA-07** Phase 2 — was "Complete" → **stays Complete**
       - **DATA-08** Phase 2 — was "Complete" → **stays Complete**
       - **DATA-09** Phase 2 — was "Complete" → **stays Complete** (profile editor + handler + mapping-editor.tsx + 6 tests)
       - **DATA-10** Phase 2 — was "Complete" → **stays Complete** (3 seeded profiles + AxiomaW1 E2E test)
       - **AUDIT-01** Phase 2 — was "Complete" → **stays Complete**
       - **CHIRP-04** Phase 2 — was "Complete" → **stays Complete** (atomic flow + handlers + dialog + serve_test)

       The pre-gap-closure statuses overstated SITE-01 / DATA-03/04/05/07/09 / CHIRP-04 — they were "Complete" in REQUIREMENTS.md but actually "Backend-Only" because no UI/wiring shipped. After the gap closure, those statuses ARE accurate (Complete in the operator-visible sense). The single status change is DATA-06 Pending → Complete.

       **Important:** Do NOT downgrade any Phase 2 requirement to "Backend-Only" or "Pending" UNLESS Task 1 evidence shows a failure. If TestScenario_AxiomaW1_E2E PASS, DATA-10 stays Complete. If TestServe_FullBoot_MQTTUplinkPersists PASS, DATA-03 stays Complete. The verifier's "overstated" flag was issued PRE-gap-closure; gap closure fixes the underlying reality.

       **MISSING-list handling (W4):** If `/tmp/02-15-testharness-results.txt` contains any `MISSING:` lines, DATA-06 stays "Pending" and the missing scenario names are recorded in 02-15-SUMMARY.md verbatim. Do NOT flip DATA-06 to Complete on partial evidence.

       Also update the v1 Requirements checklist at the top of REQUIREMENTS.md:
       - **DATA-06**: change `- [ ]` to `- [x]` — synthetic-data tests now exist (only if Task 1 W4 zero-MISSING).

    2. **Edit .planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md per-task map**:

       For each ⬜ pending row in the table at lines 46-86, flip Status to ✅ green and update the "Automated Command" column to point at the now-passing test:

       Examples (apply pattern to all rows):
       - SITE-01 row 1 (Admin can create site via dialog): `go test ./internal/site -run TestCreateSite_Admin` ⬜ → ✅
       - DATA-04 row 4 (Swap dialog reads outgoing reading R): `pnpm --dir web test routes/metering-points/swap-meter-dialog.test.tsx` ⬜ → ✅
       - DATA-06 row 1-5 (synthetic harness scenarios): `go test ./internal/testharness -run TestScenario_CleanSwap` etc. ⬜ → ✅
       - DATA-09 row 3 (mapping editor UI click decoded JSON tree): `pnpm --dir web test routes/profiles/mapping-editor.test.tsx` ⬜ → ✅
       - DATA-10 row 2 (AxiomaW1 codec end-to-end): `go test ./internal/testharness -run TestScenario_AxiomaW1_E2E` ⬜ → ✅
       - CHIRP-04 row 4 (single-action submit): `pnpm --dir web test routes/devices/add-device-dialog.test.tsx` ⬜ → ✅

       For any row where the test command did NOT actually pass (Task 1 evidence shows red or skip), mark it ❌ red or ⚠ flaky with a 1-line reason. Investigate + escalate to a follow-up plan if a fail is unexpected.

       Update VALIDATION.md sign-off section at the bottom (lines 142-150) — flip approval to "approved 2026-05-04 (gap-closure complete)" or similar.
  </action>
  <verify>
    <automated>grep -c "DATA-06.*Complete" .planning/REQUIREMENTS.md && grep -c "✅" .planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - DATA-06 status flipped: `grep -F "DATA-06" .planning/REQUIREMENTS.md | grep -F "Complete"` returns at least 1 line (the traceability row); `grep -F "DATA-06" .planning/REQUIREMENTS.md | head -1` shows `- [x]` checkbox
    - VALIDATION.md ⬜ pending rows resolved: `grep -c "⬜" .planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md` returns 0 (or only the legend line if it has one)
    - VALIDATION.md ✅ green count: `grep -c "✅" .planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md` returns at least 30 (one per requirement test row from the per-task map)
    - No ❌ red rows unless Task 1 actually showed a fail: `grep -c "❌" .planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md` returns 0 (clean run expected per Task 1 acceptance)
    - REQUIREMENTS.md v1 checklist DATA-06 marked: `grep -F "DATA-06" .planning/REQUIREMENTS.md | head -1` shows `- [x]` or similar
    - VALIDATION.md sign-off updated: `grep -F "approved" .planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md` returns at least 1 line in the sign-off block with a date >= 2026-05-04
  </acceptance_criteria>
  <done>REQUIREMENTS.md and VALIDATION.md reflect post-gap-closure reality; DATA-06 marked Complete; all ⬜ pending rows resolved.</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md + ROADMAP.md progress + commit</name>
  <read_first>
    - .planning/STATE.md (current cursor + progress)
    - .planning/ROADMAP.md (Progress table near line 205-213)
    - Tasks 1 + 2 outputs
  </read_first>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    1. **Edit .planning/ROADMAP.md Progress table** — Phase 2 row update:
       - Locate the Progress table (around line 226 — `grep -n "Phase 2.*In Progress\\|6/10" .planning/ROADMAP.md` will pin the exact line; the verifier-flagged location was approximately line 226 but the executor MUST grep first since prior edits may have shifted line numbers — I3 explicit-line reference).
       - Phase 2 row: change `6/10` to `15/15` (10 original plans + 5 gap-closure plans).
       - Phase 2 status: change `In Progress` to `Complete`.
       - Phase 2 Completed column: set to today's date (2026-05-04).

       The Phase 2 plan list at lines 84-101 already reflects 15 plans (verified at revision time — `grep -n "02-15-PLAN" .planning/ROADMAP.md` should already return a hit). Only the Progress table at the bottom of ROADMAP.md needs flipping. **If the plan list has drifted from 15 entries, re-sync it; otherwise leave alone (I3).**

       **META note (I2)** — at the top of the Phase 2 section, add a single-line comment documenting the wave numbering jump:
       ```
       > Note: gap-closure plans 02-11..15 occupy waves 7-10. Original phase template covered waves 1-6. The 3→7 jump preserves the original wave assignment for plans 02-01..10 and avoids confusing future Phase 3 wave numbering.
       ```

    2. **Edit .planning/STATE.md**:
       - Update the cursor / current-position section to show Phase 2 complete + Phase 3 next.
       - Add a brief "Phase 2 retrospective" or "Phase 2 closure" note pointing at the 5 gap-closure plans + their evidence.
       - Update any blockers / pending sections — Phase 2 should no longer be listed as a blocker for Phase 3.

       (The exact STATE.md schema is project-specific — read the file's current shape and update in place. Do NOT introduce new top-level sections; only update existing ones.)

    3. **Sanity sweep grep**:
       Confirm no remaining "Plan 02-15" or "TODO(plan-02-15)" references exist in the codebase that should have been resolved by Plans 02-12 + 02-13:
       ```bash
       grep -rn "plan-02-15\|Plan 02-15" internal/ cmd/ web/ 2>&1
       ```
       Expected: 0 lines (Plans 02-12 + 02-13 should have replaced every reference). If matches found, file an issue + add a fix-up commit before signing off.

    4. **Final commit message** (the planner-agent helper does this; document the expected message here for traceability):
       ```
       docs(phase-02): close gap-closure phase + reconcile statuses

       Plans 02-11..15 close VERIFICATION.md gaps:
       - 02-11: swap + profile HTTP route surface
       - 02-12: cmd/serve full Phase 2 boot wiring (Plan 02-15 was missing)
       - 02-13: DATA-06 synthetic test harness + shifter test-harness CLI
       - 02-14: frontend dialogs + list pages + routing
       - 02-15: REQUIREMENTS.md + VALIDATION.md status reconciliation
              + RESEARCH.md Open Questions resolved (Q1..Q5)

       Phase 2 success criteria 1-5 (ROADMAP) all proven by automated tests.
       DATA-06 flipped from Pending to Complete.
       ```
  </action>
  <verify>
    <automated>grep -F "15/15" .planning/ROADMAP.md && grep -F "Phase 2" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP Progress table updated: `grep -F "15/15" .planning/ROADMAP.md` returns 1 line; `grep -E "Phase 2.*Complete" .planning/ROADMAP.md` returns 1 line
    - Wave-jump META note present (I2): `grep -F "waves 7-10" .planning/ROADMAP.md` returns 1 line
    - Phase 2 plan list shows 15 entries: `grep -c "02-1[1-5]-PLAN.md" .planning/ROADMAP.md` returns at least 5
    - STATE.md acknowledges Phase 2 closure: `grep -F "Phase 2" .planning/STATE.md` returns at least 1 line referencing closure or completion
    - Stale Plan 02-15 references are eliminated: `grep -rn "plan-02-15\|TODO(plan-02-15)" internal/ cmd/ web/ 2>&1` returns 0 lines
    - All four .planning files modified are committed (verify with `git diff --stat .planning/`)
  </acceptance_criteria>
  <done>STATE.md + ROADMAP.md reflect Phase 2 completion; stale references purged; final commit ready.</done>
</task>

<task type="auto">
  <name>Task 4: Resolve RESEARCH.md Open Questions block (Dimension 11 hygiene blocker B1)</name>
  <read_first>
    - .planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md lines 945-971 (the `## Open Questions` block — 5 questions)
    - .planning/phases/02-domain-model-canonical-schema/02-03-SUMMARY.md (Q1 binding interval — half-open `[valid_from, valid_to)` decision; cited 0014_binding.up.sql btree_gist EXCLUDE)
    - .planning/phases/02-domain-model-canonical-schema/02-07-SUMMARY.md (Q2 concurrent swap conflict — Serializable txn first-commit-wins; Q3 rollover audit — `action: "rollover.detected"` row content; Q4 audit-diff explicit-null — line 84 of 02-07-SUMMARY explicitly states "ChangedFields produces field-level diffs with EXPLICIT-NULL semantics (Open Q #4)"; line 97 confirms `internal/audit/diff.go ChangedFields(before, after) with EXPLICIT-NULL semantics for added/removed keys")
    - .planning/phases/02-domain-model-canonical-schema/02-08-SUMMARY.md (Q5 codec_js delivery — //go:embed from internal/profile/codecs/*.js per Plan 02-08; lines 42 + 102 confirm `internal/profile/codecs/embed.go` + `2 .js` files; SQL migration 0010 inserts profile metadata with empty codec_js, RunSeedSync at boot updates it via codecs.CodecBySlug)
    - .planning/phases/02-domain-model-canonical-schema/02-09-SUMMARY.md (Q3 rollover persist branch — measurement persist with rollover audit row)
    - internal/db/migrations/0014_binding.up.sql (btree_gist EXCLUDE definition for Q1 citation)
    - internal/audit/diff.go (Q4 explicit-null implementation)
    - internal/profile/seed.go + internal/profile/codecs/embed.go (Q5 implementation)
    - internal/db/migrations/0010_seed_profiles.up.sql (Q5 — confirms migration inserts empty codec_js field; codec body comes from RunSeedSync via //go:embed)
  </read_first>
  <files>.planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md</files>
  <action>
    The plan-checker (gap-closure revision pass) flagged that 02-RESEARCH.md `## Open Questions` (lines 945-971) was never resolved despite the actual decisions having landed. This task closes that Dimension 11 hygiene gap surgically — only edit lines 945-971; do NOT touch the Assumptions block (lines 935-943) or Environment Availability (line 972+).

    1. **Flip the heading** (line 945):
       - Before: `## Open Questions`
       - After:  `## Open Questions (RESOLVED)`

    2. **Inline-mark each of the 5 questions** with `**RESOLVED:** <decision>` on a new bullet line directly under the question's existing recommendation, plus a citation to the landed implementation. Preserve the original question + "What we know" + "What's unclear" + "Recommendation" bullets verbatim — they remain useful historical context. Append a new `- **RESOLVED:** ...` bullet to each question.

       **Q1 (binding interval `[)` vs `[]`):**
       ```
       - **RESOLVED:** Half-open `[valid_from, valid_to)` per Plan 02-03 SUMMARY. The btree_gist EXCLUDE constraint in `internal/db/migrations/0014_binding.up.sql` uses tstzrange `[)` semantics; CONTEXT D-14 text was reconciled to read "gateway_rx_time < valid_to → old binding, gateway_rx_time >= valid_to → new binding." Citation: `internal/db/migrations/0014_binding.up.sql` (EXCLUDE definition) + `02-03-SUMMARY.md`.
       ```

       **Q2 (device_profile.region per-profile vs install-wide):**
       ```
       - **RESOLVED:** Per-profile column `device_profile.region TEXT NULL` (NULL = inherit from install-wide chirpstack_connection.region_name) per Plan 02-02 schema migration `0009_device_profile.up.sql`. Profile editor surfaces the override per UI-SPEC §Profile editor identity row (Plan 02-08). Citation: `internal/db/migrations/0009_device_profile.up.sql` + `02-02-SUMMARY.md`.
       ```

       *(Note: original Q2 in research lines 952-955 is about region; the gap-closure revision context's Q2 list also references "concurrent swap conflict resolution" — that semantic question maps to the original RESEARCH Q3. The numbering below follows the literal RESEARCH.md ordering.)*

       **Q3 (in-flight uplinks during swap when `gateway_rx_time` missing):**
       ```
       - **RESOLVED:** Ingest pipeline falls back to server-ingest-time (`time.Now().UTC()`) when `rxInfo[].time` is missing, AND records the substitution. Concurrent swap commits use Serializable txn with first-commit-wins semantics: the second commit hits pgconn 23P01 (exclusion_violation from the binding btree_gist EXCLUDE) and is mapped to HTTP 409 "concurrent_swap" by Plan 02-11's swap handler. Citation: Plan 02-09 SUMMARY `internal/ingest/normalize.go` (gateway_rx_time fallback) + Plan 02-07 SUMMARY `internal/swap/commit.go` + `TestCommitSwap_ConcurrentOneWins`.
       ```

       **Q4 (audit-diff explicit-null vs missing-key):**
       ```
       - **RESOLVED:** Explicit null preserved (`{field: null}` in `before`/`after` JSONB) per Plan 02-07. `internal/audit/diff.go ChangedFields(before, after)` produces field-level diffs with EXPLICIT-NULL semantics for added/removed keys (Open Q #4) — distinguishing "field absent from diff" (key not in JSONB) from "field changed FROM something TO null" (key present with null value). Phase 6 audit browse UI will render the two cases distinctly. Rollover audit rows use `action: "rollover.detected"`, `before` = previous reading snapshot, `after` = current reading + advanced offset. Citation: `internal/audit/diff.go` + `02-07-SUMMARY.md` line 84 + Plan 02-09 persist.go rollover branch.
       ```

       **Q5 (codec_js delivery — SQL inline vs //go:embed):**
       ```
       - **RESOLVED:** //go:embed from `internal/profile/codecs/*.js` per Plan 02-08. Migration 0010_seed_profiles.up.sql inserts profile metadata rows (slug + name + vendor + family + capabilities + counter_modulus + mac_version) with codec_js LEFT EMPTY; `internal/profile/codecs/embed.go` //go:embed-s the `axioma_w1.js` + `acrel_family.js` source files; `internal/profile/seed.go::RunSeedSync` at boot reads the embedded JS via `codecs.CodecBySlug(slug)`, pushes it to ChirpStack via gRPC, and persists the body + `cs_profile_id` + `codec_js_synced_at` in a Serializable txn. Citation: `internal/profile/codecs/embed.go` + `internal/profile/seed.go::RunSeedSync` + `02-08-SUMMARY.md` lines 42, 102.
       ```

    3. **Verify only lines 945-971 changed**: `git diff .planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md` should show edits confined to that block plus added RESOLVED bullets. The Assumptions table (A1-A8) and Environment Availability sections must NOT be touched.
  </action>
  <verify>
    <automated>grep -F "## Open Questions (RESOLVED)" .planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md && grep -c "RESOLVED:" .planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md</automated>
  </verify>
  <acceptance_criteria>
    - Heading flipped: `grep -F "## Open Questions (RESOLVED)" .planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md` returns 1 line
    - Original heading gone: `grep -E "^## Open Questions$" .planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md` returns 0 lines
    - 5 RESOLVED bullets present: `grep -c "**RESOLVED:**" .planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md` returns at least 5
    - Each RESOLVED references a citation: `grep -E "Citation:|internal/" .planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md | grep -A0 RESOLVED` shows linkage (loose check — visual inspection during diff review)
    - Q4 explicitly cites 02-07-SUMMARY: `grep -F "EXPLICIT-NULL" .planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md` returns at least 1 line referencing Plan 02-07
    - Q5 explicitly cites //go:embed + embed.go: `grep -F "//go:embed" .planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md` returns at least 1 line; `grep -F "codecs/embed.go" .planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md` returns at least 1 line
    - Surgical scope: `git diff --stat .planning/phases/02-domain-model-canonical-schema/02-RESEARCH.md` shows < 50 lines changed (only the Open Questions block edited; Assumptions A1-A8 + Environment Availability untouched)
  </acceptance_criteria>
  <done>RESEARCH.md `## Open Questions (RESOLVED)` heading flipped + 5 inline RESOLVED bullets with citations to landed implementations. Phase-wide Dimension 11 hygiene blocker B1 closed. A future verifier or human reviewer sees the research thread closed cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies only documentation files in .planning/. No production code or schema changes; no security boundaries crossed. STRIDE register is intentionally minimal.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15-01 | Repudiation | Status flips not backed by evidence | mitigate | Task 1 captures evidence logs to /tmp; Tasks 2+3+4 only flip statuses Task 1 confirmed green; ❌/⚠ used when evidence is fail/flaky; W4 per-test loop prevents partial-evidence completion claims |
| T-02-15-02 | Information disclosure | Evidence logs in /tmp leak credentials | accept | testcontainer runs use ephemeral creds; logs are local to dev machine; not committed |
| T-02-15-03 | Tampering | RESEARCH.md edits accidentally rewrite Assumptions / Environment blocks | mitigate | Task 4 acceptance enforces "< 50 lines changed" cap + git diff review confines edits to lines 945-971 |
</threat_model>

<verification>
After all 4 tasks:

1. `git diff --stat .planning/` shows 5 files modified: REQUIREMENTS.md, VALIDATION.md, RESEARCH.md, STATE.md, ROADMAP.md
2. No code regressions: re-run `go test ./... -short -count=1` exits 0 (sanity)
3. No code regressions: re-run `pnpm --dir web test --run` exits 0
4. ROADMAP shows Phase 2 complete with 15 plans + wave-jump META note
5. REQUIREMENTS.md DATA-06 marked complete
6. RESEARCH.md Open Questions block flipped to (RESOLVED) with 5 inline RESOLVED bullets + citations
7. No stale Plan 02-15 references in code: `grep -rn "plan-02-15" internal/ cmd/ web/` returns 0
</verification>

<success_criteria>
- 5 .planning files updated reflecting post-gap-closure reality
- DATA-06 status: Pending → Complete with evidence (TestScenario_* commands, W4 per-test loop confirmed)
- VALIDATION.md ⬜ pending rows: 0 remaining
- ROADMAP Phase 2: 6/10 In Progress → 15/15 Complete (or correct final count); I2 wave-jump note added
- RESEARCH.md Open Questions: heading flipped to (RESOLVED) + 5 RESOLVED bullets with citations
- STATE.md cursor advances past Phase 2
- Stale Plan 02-15 references purged from production code
- Final commit message documents the 5 gap-closure plans + outcome including RESEARCH.md hygiene fix
</success_criteria>

<output>
After completion, create `.planning/phases/02-domain-model-canonical-schema/02-15-SUMMARY.md` documenting:
- 5 .planning files modified (REQUIREMENTS.md, VALIDATION.md, RESEARCH.md, STATE.md, ROADMAP.md)
- Status flip table: which requirements changed, which stayed, evidence per row
- W4 per-test results: include the contents of `/tmp/02-15-testharness-results.txt` verbatim (or note "all 6 scenarios PASS, no MISSING") for traceability
- Sanity test re-run snippet (the second go test ./... + pnpm test pass count)
- Confirmation that VERIFICATION.md gaps frontmatter `gaps: []` would now be empty if re-run
- RESEARCH.md Q1-Q5 RESOLVED summary table mapping each question to its landed implementation citation
- Pointer to next phase (Phase 3 — Provisioning) per ROADMAP linear chain
- Optional Phase 2 mini-retrospective: what worked (vertical-slice plans landed cleanly; gap closure was the cost of skipping the boot-wiring plan in the original phase template), what to avoid in Phase 3 (always include the cmd/serve wiring task as the LAST plan of every phase template, not as an implicit "later" — Plan 02-15's missing-from-template was the single root cause of all 3 verification gaps; also: include an Open Questions resolution sweep in every phase's final reconciliation plan, not as an afterthought)
</output>
