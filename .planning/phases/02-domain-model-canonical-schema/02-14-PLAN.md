---
phase: 02-domain-model-canonical-schema
plan: 14
type: execute
gap_closure: true
wave: 8
depends_on: [02-10, 02-11]
files_modified:
  - web/src/App.tsx
  - web/src/lib/api.ts
  - web/src/components/shell/sidebar.tsx
  - web/src/routes/sites/index.tsx
  - web/src/routes/sites/create-site-dialog.tsx
  - web/src/routes/sites/create-site-dialog.test.tsx
  - web/src/routes/sites/$id.tsx
  - web/src/routes/metering-points/$id.tsx
  - web/src/routes/metering-points/create-mp-dialog.tsx
  - web/src/routes/metering-points/swap-meter-dialog.tsx
  - web/src/routes/metering-points/swap-meter-dialog.test.tsx
  - web/src/routes/devices/index.tsx
  - web/src/routes/devices/add-device-dialog.tsx
  - web/src/routes/devices/add-device-dialog.test.tsx
  - web/src/routes/devices/deveui-parser.tsx
  - web/src/routes/devices/deveui-parser.test.tsx
  - web/src/routes/profiles/index.tsx
  - web/src/routes/profiles/$id.tsx
  - web/src/routes/profiles/mapping-editor.tsx
  - web/src/routes/profiles/mapping-editor.test.tsx
  - web/components.json
  - web/package.json
autonomous: false
requirements: [SITE-01, DATA-01, DATA-04, DATA-09, CHIRP-04]

must_haves:
  truths:
    - "Operator can navigate to /sites, /devices, /profiles, /metering-points/{id} from the sidebar; each shows the list/detail UI defined in 02-UI-SPEC.md."
    - "Add Site dialog (D-17 + D-18) opens from /sites; form fields per UI-SPEC; on submit calls POST /api/sites; success closes dialog + invalidates query + shows toast."
    - "Add Metering Point dialog (D-19) opens from site detail; on submit calls POST /api/metering-points."
    - "Add Device 4-step Stepped dialog (D-10 + D-11 + D-16) opens from /devices; step 1 uses DevEUIParser component to show MSB+LSB previews + OUI hint; step 4 runs CS pre-flight; submit calls POST /api/devices."
    - "Swap Meter 3-step Stepped dialog (D-12 + D-13 + D-14) opens from MP detail; step 1 auto-fills R from latest measurement; step 3 shows the math read-back panel; submit calls POST /api/metering-points/{id}/swap."
    - "Mapping Editor (D-08) opens at /profiles/{id} OR /profiles/new; left pane = paste JSON + clickable tree; right pane = TanStack Table mapping rows; save calls POST or PATCH /api/device-profiles."
    - "Vitest skip placeholders (5 files) flipped to active tests; all 5 tests pass via @testing-library/react."
    - "Sidebar inserts Sites + Devices + Profiles nav items in the order specified by 02-UI-SPEC §Layout."
  artifacts:
    - path: "web/src/routes/sites/index.tsx"
      provides: "Sites list page; TanStack Table; primary 'Add site' button opens CreateSiteDialog"
      exports: ["default"]
    - path: "web/src/routes/sites/create-site-dialog.tsx"
      provides: "Modal for D-17 + D-18 — name + lat/lng + timezone + address + description; 'Pick on map' rendered disabled with tooltip 'Available in v5'"
      exports: ["CreateSiteDialog"]
    - path: "web/src/routes/sites/$id.tsx"
      provides: "Site detail page — header + identity card + nested MP list (with create CTA) + nested devices list scoped to site"
      exports: ["default"]
    - path: "web/src/routes/metering-points/$id.tsx"
      provides: "MP detail page — header + latest reading card + active binding card with Swap meter + Decommission action menu"
      exports: ["default"]
    - path: "web/src/routes/metering-points/create-mp-dialog.tsx"
      provides: "D-19 dialog — name, site, utility_class radio, location_description"
      exports: ["CreateMeteringPointDialog"]
    - path: "web/src/routes/metering-points/swap-meter-dialog.tsx"
      provides: "3-step Stepper dialog (D-12/13/14) — outgoing reading capture + new device picker + math read-back + Confirm swap"
      exports: ["SwapMeterDialog"]
    - path: "web/src/routes/devices/index.tsx"
      provides: "Devices list page; TanStack Table; primary 'Add device' button"
      exports: ["default"]
    - path: "web/src/routes/devices/add-device-dialog.tsx"
      provides: "4-step Stepper dialog (D-10) — paste DevEUI, pick profile, bind to MP, review + submit with CS pre-flight"
      exports: ["AddDeviceDialog"]
    - path: "web/src/routes/devices/deveui-parser.tsx"
      provides: "D-11 reusable component — MSB+LSB radio rows + vendor OUI hint; emits onPick(devEUI)"
      exports: ["DevEUIParser"]
    - path: "web/src/routes/profiles/index.tsx"
      provides: "Profiles list page; TanStack Table — name/vendor/family/capabilities chips/sync status"
      exports: ["default"]
    - path: "web/src/routes/profiles/$id.tsx"
      provides: "Profile editor wrapper — mounts MappingEditor; handles new vs edit"
      exports: ["default"]
    - path: "web/src/routes/profiles/mapping-editor.tsx"
      provides: "D-08 two-pane editor — left: sample JSON tree clickable; right: mapping rows TanStack Table + codec_js textarea + capability checkboxes; save calls POST/PATCH /api/device-profiles"
      exports: ["MappingEditor"]
    - path: "web/src/App.tsx"
      provides: "router updated with /sites, /sites/:id, /metering-points/:id, /devices, /profiles, /profiles/new, /profiles/:id"
  key_links:
    - from: "web/src/routes/sites/create-site-dialog.tsx"
      to: "POST /api/sites"
      via: "apiFetch with name + lat + lng + timezone + address + description"
      pattern: "apiFetch.*'/api/sites'"
    - from: "web/src/routes/devices/add-device-dialog.tsx"
      to: "POST /api/devices/preflight + POST /api/devices/parse-deveui + POST /api/devices"
      via: "useMutation calls in step 4 (preflight) + step 1 (parse-deveui via DevEUIParser) + final submit"
      pattern: "apiFetch.*'/api/devices"
    - from: "web/src/routes/metering-points/swap-meter-dialog.tsx"
      to: "GET /api/metering-points/{id} + POST /api/metering-points/{id}/swap + GET /api/devices/search"
      via: "auto-fill R from latest_measurement; device picker; submit"
      pattern: "apiFetch.*'/api/metering-points/.*/swap'"
    - from: "web/src/routes/profiles/mapping-editor.tsx"
      to: "GET /api/device-profiles/{id} + POST /api/device-profiles/{id}/decoded-sample + POST /api/device-profiles + PATCH /api/device-profiles/{id}"
      via: "load + sample preview + save"
      pattern: "apiFetch.*'/api/device-profiles"
---

<objective>
Land every Phase 2 frontend surface listed in 02-UI-SPEC.md. Verifier flagged ALL 5 dialogs as Wave-0 placeholders + ALL list pages as missing + App.tsx as containing zero Phase 2 routes. Without this plan, the operator has zero way to drive the headline phase deliverables (create site, add device, swap meter, edit profile mapping). This plan ships every dialog + list page + routing entry per UI-SPEC's anatomy contracts and flips the 5 vitest skip stubs to active tests.

Purpose: Closes the frontend half of VERIFICATION.md Truths 1, 3 + DATA-09 "through a UI" half + the operator-reachability of CHIRP-04 + SITE-01 + DATA-04. After this plan, every backend route Plan 02-10 + 02-11 mounted has a UI caller.

Output: 11 new .tsx component files + 5 new test files (replacing skip stubs) + 2 new route layout files + edits to App.tsx, sidebar.tsx, api.ts, components.json (new shadcn primitives), package.json (new deps). Wave 8: depends on Plan 02-11 (HTTP routes for swap + profile must exist) and Plan 02-10 (site/MP/device routes already shipped). Does NOT depend on Plan 02-12 because the integration test in 02-12 is what GLUES this dialog set to the production binary; the dialogs themselves are testable against MSW or direct apiFetch in vitest without serve.go running.

**Scope sanity (W2 from gap-closure revision):** This plan touches 22 files / 11 components — at the upper edge of single-plan tractability. The plan-checker offered two refactors: (a) split 02-14 into 02-14a + 02-14b, or (b) keep 3 tasks but insert an explicit between-task checkpoint that pins commit-and-vitest after Task 2 before Task 3 begins. **Choice: (b)**. Reasoning: ROADMAP.md plan list already at 15 plans (matching CONTEXT-locked plan count); option (a) bumps to 16 and forces a ROADMAP edit cascading into Plan 02-15's reconciliation. Option (b) achieves the same execution-safety guarantee — Task 3 begins with a known-good Task-2 codebase + vitest baseline — without inflating the plan count. The plan is now `autonomous: false` and the checkpoint protocol below is binding.
</objective>

<checkpoint_protocol>
## Between-Task Checkpoint Protocol (W2 option (b))

This plan is `autonomous: false` because Tasks 2 and 3 share frontend dependencies (shadcn primitives installed by Task 1; reusable components introduced by Task 2 and consumed by Task 3) and Task 3 cannot meaningfully begin until Task 2's components are committed and vitest is green. Without the checkpoint, an executor that runs all 3 tasks in one context window risks Task 3 reasoning against in-flight (uncommitted, possibly broken) Task 2 state.

**After Task 1 completes** (sites surface + shadcn primitives + sidebar):
- Run `pnpm --dir web test routes/sites --run` — must exit 0.
- Run `pnpm --dir web build` — must exit 0.
- Commit Task 1 changes with message `feat(02-14): sites surface + shadcn primitives + sidebar nav`.

**After Task 2 completes** (MP + Devices + DevEUI parser + 3 dialogs):
- **HARD STOP — DO NOT begin Task 3 in the same context window.**
- Run `pnpm --dir web test routes/devices routes/metering-points --run` — must exit 0.
- Run `pnpm --dir web test --run` (full vitest) — must exit 0.
- Run `pnpm --dir web build` — must exit 0.
- Commit Task 2 changes with message `feat(02-14): MP + Devices + 3 dialogs (Add MP, Add Device 4-step, Swap Meter 3-step) + DevEUI parser`.
- After commit, the executor SHOULD `/clear` (fresh context window) before starting Task 3.

**Task 3 entry protocol** (when starting fresh-context Task 3):
- Re-read the latest commit's component files via `git log -1 --stat` to identify exactly what landed.
- Re-read the SHA-pinned files in Task 3's `<read_first>` block (App.tsx, sidebar.tsx, api.ts) plus the new files Task 2 produced (add-device-dialog.tsx, swap-meter-dialog.tsx, deveui-parser.tsx, create-mp-dialog.tsx).
- Re-read 02-UI-SPEC.md §Profile editor page (the Task 3 anatomy contract).
- Confirm `pnpm --dir web test --run` is green BEFORE adding any Task 3 changes — if red, fix Task 2 regressions first as a small follow-up commit.

**Why this matters:** Task 2 introduces 11 components (DevEUI parser + 3 dialogs + MP detail + Devices list + 4 lib helpers + supporting files). Combined with Task 1's 6 shadcn primitives, the cumulative file-touch budget for a single context window is at the edge. Task 3's mapping editor is non-trivial on its own (two-pane editor + JSON flatten helper + 6 vitest tests) and needs its own ~50% context budget. The checkpoint converts a "monolithic 22-file plan" into "three sub-plans separated by commits + fresh-context handoffs" without inflating the plan-list count.
</checkpoint_protocol>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-domain-model-canonical-schema/02-CONTEXT.md
@.planning/phases/02-domain-model-canonical-schema/02-UI-SPEC.md
@.planning/phases/02-domain-model-canonical-schema/02-VERIFICATION.md
@.planning/phases/02-domain-model-canonical-schema/02-VALIDATION.md
@web/src/App.tsx
@web/src/main.tsx
@web/src/lib/api.ts
@web/src/components/shell/sidebar.tsx
@web/src/components/responsive-dialog.tsx
@web/src/components/stepper.tsx
@web/src/components/status-row.tsx
@web/src/routes/_root.tsx
@web/src/routes/settings.tsx
@web/src/routes/sites/create-site-dialog.test.tsx
@web/src/routes/devices/add-device-dialog.test.tsx
@web/src/routes/devices/deveui-parser.test.tsx
@web/src/routes/metering-points/swap-meter-dialog.test.tsx
@web/src/routes/profiles/mapping-editor.test.tsx
@internal/site/handlers.go
@internal/meteringpoint/handlers.go
@internal/device/handlers.go
@internal/swap/handlers.go
@internal/profile/handlers.go

<interfaces>
<!-- Backend route shapes the dialogs call. Names + bodies confirmed by the handlers.go files referenced above. -->

POST /api/sites — admin only
  body: { name, lat?, lng?, timezone, address?, description?, parent_id?, site_type? }
  response 201: { id, name, lat, lng, timezone, ... }

POST /api/metering-points — admin only
  body: { site_id, name, utility_class: "water"|"electricity", location_description? }
  response 201: { id, name, ... }

POST /api/devices/preflight — admin only
  body: {} (the handler reads cfg)
  response 200: { grpc: "ok"|"err", mqtt: "ok"|"err", grpc_detail?, mqtt_detail? }

POST /api/devices/parse-deveui — admin + viewer
  body: { raw: string }
  response 200: { msb, lsb, msb_vendor, lsb_vendor }

POST /api/devices — admin only (CHIRP-04 atomic)
  body: { dev_eui, name, device_profile_id, app_key, join_eui?, description?, metering_point_id?, initial_reading? }
  response 201: { id, ... }

POST /api/devices/{id}/decommission — admin only
  body: {}
  response 200: { binding_closed: true }

POST /api/metering-points/{id}/swap — admin only (Plan 02-11)
  body: { incoming_device_id, outgoing_reading_r, incoming_initial_n?, operator_override?, operator_notes? }
  response 200: { binding_id }
  response 409: { error: "concurrent_swap" }

GET /api/device-profiles — admin + viewer
GET /api/device-profiles/{id} — admin + viewer; returns { profile, mappings }
POST /api/device-profiles/{id}/decoded-sample — admin + viewer; body { sample: any }; response { leaves: [{ json_pointer, value }] }
POST /api/device-profiles — admin only; body matches ProfileRequest from Plan 02-11
PATCH /api/device-profiles/{id} — admin only

GET /api/metering-points/{id} returns { mp, active_binding: { device, profile, reading_offset, last_measurement: { cumulative_value, time, raw_value } } } — Plan 02-10's getMPDetail handler.

From web/src/components/responsive-dialog.tsx (Phase 1):
```tsx
export interface ResponsiveDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  title: string;
  description?: string;
  children: React.ReactNode;
  size?: "md" | "lg";  // sm:max-w-2xl for "lg", default for "md"
}
export function ResponsiveDialog(props: ResponsiveDialogProps): JSX.Element
```

From web/src/components/stepper.tsx (Phase 1):
```tsx
export interface StepperProps {
  steps: Array<{ id: string; label: string }>;
  currentStep: number;  // 0-indexed
}
export function Stepper(props: StepperProps): JSX.Element
```

From web/src/components/status-row.tsx (Phase 1):
```tsx
export interface StatusRowProps {
  status: "ok" | "err" | "pending" | "skipped";
  label: string;
  detail?: string;  // mono detail
}
export function StatusRow(props: StatusRowProps): JSX.Element
```

From web/src/lib/api.ts (Phase 1):
```ts
export async function apiFetch<T>(path: string, init?: RequestInit): Promise<T>
// Throws ApiError on non-2xx with { status, body }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shadcn primitives + nav scaffolding + sites surface</name>
  <read_first>
    - web/components.json (existing shadcn config)
    - web/src/components/shell/sidebar.tsx (Phase 1 nav anatomy)
    - web/src/App.tsx (router shape)
    - web/src/routes/sites/create-site-dialog.test.tsx (current placeholder — replace)
    - 02-UI-SPEC.md §Sites list page + §Site detail page + §Add Site dialog (anatomy + copy verbatim)
    - 02-UI-SPEC.md §shadcn components added in Phase 2 (table, command, popover, radio-group, textarea, scroll-area)
    - internal/site/handlers.go (request/response shapes)
  </read_first>
  <files>web/package.json, web/components.json, web/src/components/ui/table.tsx, web/src/components/ui/command.tsx, web/src/components/ui/popover.tsx, web/src/components/ui/radio-group.tsx, web/src/components/ui/textarea.tsx, web/src/components/ui/scroll-area.tsx, web/src/lib/api.ts, web/src/components/shell/sidebar.tsx, web/src/routes/sites/index.tsx, web/src/routes/sites/$id.tsx, web/src/routes/sites/create-site-dialog.tsx, web/src/routes/sites/create-site-dialog.test.tsx, web/src/App.tsx</files>
  <behavior>
    - Test 1 (CreateSiteDialog renders & submits): renders dialog, fills name=Foo + lat=13.7563 + lng=100.5018 + timezone=Asia/Bangkok; vi.fn() apiFetch returns { id: "uuid" } → onOpenChange called with false + onCreated called with new site id.
    - Test 2 (CreateSiteDialog validates lat/lng range): lat=99 → form error "Latitude must be between −90 and 90" inline beneath field; submit button disabled until valid.
    - Test 3 (CreateSiteDialog renders disabled 'Pick on map' button): the button is in the DOM with `disabled` attribute and tooltip text "Available in v5".
    - Test 4 (CreateSiteDialog matches D-18 helper text): the lat/lng pair has helper text containing "paste from Google Maps".
  </behavior>
  <action>
    1. **Install shadcn primitives** (per 02-UI-SPEC § shadcn components added in Phase 2). Add the 6 primitives to web/components.json's blocks list and run `pnpm dlx shadcn@latest add table command popover radio-group textarea scroll-area`. Confirm they land in web/src/components/ui/.

    2. **Add npm dependencies** to web/package.json:
       - `@tanstack/react-table` (already added per Plan 02-01 SUMMARY)
       - confirm peer deps for `cmdk` (command palette base)
       Run `pnpm install`.

    3. **Add API helpers** to web/src/lib/api.ts (or a new web/src/lib/sites.ts that re-exports from api.ts — your call, mirror the existing settings.ts / install.ts pattern):
       ```ts
       export type Site = { id: string; name: string; lat?: number; lng?: number; timezone: string; address?: string; description?: string; parent_id?: string; archived_at?: string | null };
       export type CreateSiteRequest = Omit<Site, 'id' | 'archived_at'> & { parent_id?: string };

       export async function listSites(opts: { archived?: boolean } = {}): Promise<Site[]> {
         return apiFetch<Site[]>(opts.archived ? '/api/sites/archived' : '/api/sites');
       }
       export async function createSite(body: CreateSiteRequest): Promise<Site> {
         return apiFetch<Site>('/api/sites', { method: 'POST', body: JSON.stringify(body), headers: { 'Content-Type': 'application/json' } });
       }
       // similar for getSite, updateSite, archiveSite, restoreSite
       ```

    4. **Sidebar** — edit web/src/components/shell/sidebar.tsx to insert 3 new nav items in the order from 02-UI-SPEC §Layout (Sites, Devices, Profiles, Settings). Use lucide icons MapPin, Cpu, Layers per UI-SPEC §Iconography. Each is a NavLink to /sites, /devices, /profiles.

    5. **Create web/src/routes/sites/index.tsx** — list page per 02-UI-SPEC §Sites list page anatomy:
       - Page header: `<h1 className="text-2xl font-semibold">Sites</h1>` + Plus-leading `Add site` button (variant="default") opens CreateSiteDialog.
       - Toolbar: `<Input placeholder="Search sites by name" className="max-w-sm" />` + shadcn Tabs `[All] [Archived]`.
       - Table: TanStack Table + shadcn Table primitives. Columns per UI-SPEC: Name (link to /sites/:id), Address, Metering points (count — load via GET /api/metering-points/by-site/{id} OR include count in list response if backend supports; if not, render "—" with TODO comment "metering point count requires Plan 02-1X enhancement"), Last uplink (relative time string from latest_measurement), Status (StatusRow `ok`/`warn`/`err`), Actions (DropdownMenu with Edit / Archive).
       - Empty state: heading "No sites yet" + body + primary CTA (copy verbatim from UI-SPEC).
       - Loading state: 5 `<Skeleton>` rows.
       - Use TanStack Query: `useQuery({ queryKey: ['sites'], queryFn: listSites })`. After create/archive: `queryClient.invalidateQueries({ queryKey: ['sites'] })`.

    6. **Create web/src/routes/sites/$id.tsx** — detail page per UI-SPEC §Site detail page:
       - Header with site name + address + Edit + Archive overflow.
       - Identity card with Timezone + Coordinates (mono) + Description + disabled "Pick on map" button (Map icon + Tooltip "Available in v5").
       - Metering Points section with `Add metering point` button → opens CreateMeteringPointDialog (Task 2 owns this dialog; here you just stub the import path and pass siteId prop). Table of MPs scoped to this site. Empty state per UI-SPEC.
       - Devices on this site section — read-only TanStack Table. Empty state per UI-SPEC.

    7. **Create web/src/routes/sites/create-site-dialog.tsx** — D-17 + D-18:
       - Component shape: `<CreateSiteDialog open onOpenChange onCreated />`.
       - Uses `<ResponsiveDialog>` size="md".
       - Title "Add site" / submit "Add site" / loading "Adding site…" (copy verbatim per UI-SPEC §Phase 2 copy table).
       - Form: react-hook-form + zod schema. Fields:
         - name (required, 1-100 chars)
         - lat (optional, number, range [−90, 90])
         - lng (optional, number, range [−180, 180])
         - timezone (default "Asia/Bangkok" from install — load via GET /api/install/identity? or from settings? Phase 1 has install identity stored — use a sensible default, ALL stripped to "UTC" if no install identity surfaced; the executor picks the right source by reading web/src/lib/settings.ts or web/src/lib/install.ts).
         - address (optional, textarea, 1-2 lines)
         - description (optional, textarea)
       - lat/lng grid layout: `grid grid-cols-2 gap-4` per UI-SPEC §Form patterns.
       - Helper text under lat/lng pair: "Paste from Google Maps — example: `13.7563, 100.5018`."
       - **'Pick on map' button**: `<Button variant="outline" disabled type="button">Pick on map</Button>` with `<Tooltip>Available in v5</Tooltip>` wrapper. Required by D-18.
       - Submit handler: `useMutation({ mutationFn: createSite, onSuccess: (created) => { onCreated?.(created); onOpenChange(false); toast.success("Site created"); }, onError: (err) => { toast.error("Could not create site"); /* show inline alert */ } })`.

    8. **Replace web/src/routes/sites/create-site-dialog.test.tsx** — flip from placeholder skip to active tests. Use @testing-library/react + vitest. Mock apiFetch via msw or vi.mock. Tests as in <behavior> above.

    9. **Edit web/src/App.tsx** — add routes inside the RootLayout children block:
       ```tsx
       { path: 'sites', element: <Suspense fallback={null}><SitesPage /></Suspense> },
       { path: 'sites/:id', element: <Suspense fallback={null}><SiteDetailPage /></Suspense> },
       ```
       Add lazy imports:
       ```tsx
       const SitesPage = lazy(() => import('@/routes/sites'));
       const SiteDetailPage = lazy(() => import('@/routes/sites/$id'));
       ```
       (React Router uses path-prefixed file naming — adjust to your convention; if not using $id, use a flat `sites-detail.tsx` and route param `:id`.)

    Use the Phase 1 navy OKLCH palette + shadcn new-york preset that the codebase already inherits — do not introduce new color tokens.
  </action>
  <verify>
    <automated>pnpm --dir web test routes/sites --run && pnpm --dir web build</automated>
  </verify>
  <acceptance_criteria>
    - 6 shadcn primitives present: `ls web/src/components/ui/{table,command,popover,radio-group,textarea,scroll-area}.tsx` returns 6 files
    - `web/src/routes/sites/index.tsx` exists; `grep -F "Add site" web/src/routes/sites/index.tsx` returns at least 1 line
    - `web/src/routes/sites/create-site-dialog.tsx` exists; `grep -F "Pick on map" web/src/routes/sites/create-site-dialog.tsx` returns 1 line; `grep -F "disabled" web/src/routes/sites/create-site-dialog.tsx` returns at least 1 line on the same nearby block
    - `web/src/routes/sites/create-site-dialog.tsx` references "/api/sites" path: `grep -F "'/api/sites'" web/src/routes/sites/create-site-dialog.tsx` returns at least 1 line
    - `web/src/App.tsx` has /sites + /sites/:id routes: `grep -F "path: 'sites'" web/src/App.tsx` returns 1 line; `grep -F "sites/:id" web/src/App.tsx` returns 1 line
    - Sidebar updated: `grep -F "Sites" web/src/components/shell/sidebar.tsx` returns 1 line; `grep -F "MapPin" web/src/components/shell/sidebar.tsx` returns 1 line
    - All 4 named tests in create-site-dialog.test.tsx PASS: `pnpm --dir web test routes/sites/create-site-dialog --run` exits 0 with 4 passed
    - `pnpm --dir web build` exits 0 (full SPA still builds)
    - `pnpm --dir web typecheck` (or `tsc --noEmit`) clean (no TS errors introduced)
    - **W2 checkpoint:** Task 1 changes committed before Task 2 begins.
  </acceptance_criteria>
  <done>Sites list + detail + create dialog ship; sidebar nav added; routes wired in App.tsx; 4 tests pass; bundle still builds. Task 1 commit landed; ready for Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint A: Verify Task 1 commit + green vitest before starting Task 2</name>
  <what-built>Task 1 — sites surface (list + detail + create dialog), 6 shadcn primitives, sidebar nav with 3 new entries, App.tsx routes for /sites + /sites/:id.</what-built>
  <how-to-verify>
    1. Confirm the Task 1 commit landed: `git log -1 --oneline` should show `feat(02-14): sites surface + shadcn primitives + sidebar nav` (or similar message).
    2. Run `pnpm --dir web test routes/sites --run` — expect 4 PASS (CreateSiteDialog tests).
    3. Run `pnpm --dir web test --run` — expect 0 fails; the previously-skipped 4 Phase 2 dialog test files (devices/add-device-dialog, devices/deveui-parser, metering-points/swap-meter-dialog, profiles/mapping-editor) ARE STILL SKIPPED at this point — they get flipped in Tasks 2 + 3.
    4. Run `pnpm --dir web build` — expect 0 errors.
    5. Visually open http://localhost:5173/sites in the dev server (`pnpm --dir web dev`) — the Sites page renders the empty-state copy + the "Add site" button opens the dialog. (Optional visual smoke; skip if dev server unavailable.)
  </how-to-verify>
  <resume-signal>Type "approved" to start Task 2, or describe the failure (test name, error message).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Metering Points + Devices surfaces (CRUD dialogs + lists + DevEUI parser)</name>
  <read_first>
    - 02-UI-SPEC.md §Metering Point detail page + §Add MP dialog + §Devices list + §Add Device dialog + §Decommission Device dialog (anatomy + copy verbatim)
    - 02-UI-SPEC.md §Add Device dialog 4-step body (each step's heading + fields + validation + submit copy)
    - web/src/routes/devices/add-device-dialog.test.tsx (current placeholder — replace)
    - web/src/routes/devices/deveui-parser.test.tsx (current placeholder — replace)
    - web/src/routes/metering-points/swap-meter-dialog.test.tsx (current placeholder — replace)
    - web/src/components/stepper.tsx (Phase 1 — exact API)
    - web/src/components/responsive-dialog.tsx
    - internal/device/handlers.go (parseDevEUIHandler response shape; preflightCS response shape; addDevice request shape)
    - internal/swap/handlers.go (SwapRequest body shape from Plan 02-11)
    - internal/meteringpoint/handlers.go (CreateMPRequest + GetMPDetail response shape)
    - Task 1 output (Sites surface — pattern templates to mirror); recover via `git log -1 --stat` if entering Task 2 in a fresh context window per W2 checkpoint protocol
  </read_first>
  <files>web/src/lib/devices.ts, web/src/lib/metering-points.ts, web/src/lib/swap.ts, web/src/routes/metering-points/$id.tsx, web/src/routes/metering-points/create-mp-dialog.tsx, web/src/routes/metering-points/swap-meter-dialog.tsx, web/src/routes/metering-points/swap-meter-dialog.test.tsx, web/src/routes/devices/index.tsx, web/src/routes/devices/add-device-dialog.tsx, web/src/routes/devices/add-device-dialog.test.tsx, web/src/routes/devices/deveui-parser.tsx, web/src/routes/devices/deveui-parser.test.tsx, web/src/App.tsx</files>
  <behavior>
    DevEUIParser tests:
    - Test 1 (DevEUIParser_RendersBothInterpretations): rendered with raw="0102030405060708"; finds 2 radio rows: one labeled "MSB-first: 01:02:03:04:05:06:07:08" + one labeled "LSB-first: 08:07:06:05:04:03:02:01".
    - Test 2 (DevEUIParser_StripsHyphensAndColons): raw="01:02:03-04 05:06:07:08" → after blur normalization, displays the same 16-char hex pair as Test 1.
    - Test 3 (DevEUIParser_ShowsVendorOUIHint): when MSB OUI matches a known vendor (mocked apiFetch returns msb_vendor: "Axioma"), the row shows "(Vendor: Axioma)" trailing text.
    - Test 4 (DevEUIParser_OnPick_EmitsLowercaseHex): clicking the LSB radio row + clicking confirm calls onPick("0807060504030201") with the lowercase, no-separator string.

    AddDeviceDialog tests:
    - Test 1 (AddDeviceDialog_StepFlow): walks the 4 steps; each step renders the heading copy from UI-SPEC verbatim ("Paste the DevEUI from the meter sticker", "Choose a device profile", "Bind to a metering point", "Review and add").
    - Test 2 (AddDeviceDialog_PreflightFailsBlocksSubmit): step 4 preflight returns { grpc: "err" } → submit button disabled + alert "ChirpStack is unreachable. Open Settings → Test connection." rendered.
    - Test 3 (AddDeviceDialog_HappyPathSubmits): walks all 4 steps with mocked APIs; final submit POSTs /api/devices and onCreated callback fires with new device id; toast "Device {name} added and bound to {MP name}."

    SwapMeterDialog tests:
    - Test 1 (SwapMeterDialog_AutoFillsR): given mocked GET /api/metering-points/{id} returning latest_measurement.cumulative_value=12345, step 1 shows "Outgoing reading: 12,345 m³ from uplink N min ago".
    - Test 2 (SwapMeterDialog_VerifyCheckboxRequired): submit button on step 1 is disabled until "I verified this matches the physical meter." checkbox is checked.
    - Test 3 (SwapMeterDialog_MathReadback): step 3 shows three rows: "Outgoing reading R = 12345", "New meter initial N = 0", "Proposed offset = R − N = 12345" (numbers and labels per UI-SPEC).
    - Test 4 (SwapMeterDialog_SubmitCallsSwapAPI): submit calls POST /api/metering-points/{id}/swap with the right body; on 200, dialog closes + toast "Meter swapped on {MP name}."
  </behavior>
  <action>
    1. **API helpers**:
       - `web/src/lib/devices.ts` — listDevices, searchDevices, getDevice, parseDevEUI({raw}), preflight(), addDevice({...}), decommissionDevice(id).
       - `web/src/lib/metering-points.ts` — listMPs, getMPDetail (returns { mp, active_binding }), createMP, updateMP, archiveMP.
       - `web/src/lib/swap.ts` — commitSwap(mpId, body).

    2. **Create web/src/routes/metering-points/$id.tsx** — detail page per UI-SPEC §MP detail. Render header + breadcrumb + Edit + overflow menu (Archive, Swap meter, Decommission device). Latest reading card with `text-2xl font-mono` cumulative + unit. Active binding card with Device + DevEUI mono + Profile + Capabilities chips + Reading offset mono + valid_from + primary `Swap meter` opens SwapMeterDialog. Decommission opens AlertDialog. Empty binding state with "No device bound" + Add device CTA opens AddDeviceDialog with this MP pre-selected.

    3. **Create web/src/routes/metering-points/create-mp-dialog.tsx** — D-19 dialog. Fields: name + site (Select listing all non-archived sites) + utility_class (RadioGroup with "Water"/"Electricity") + location_description (Textarea). Title "Add metering point". Submit calls createMP with site_id from prop or from select.

    4. **Create web/src/routes/devices/deveui-parser.tsx** — D-11 reusable component:
       ```tsx
       export interface DevEUIParserProps {
         onPick: (devEUI: string) => void;  // emits lowercase 16-hex no-separator
       }

       export function DevEUIParser({ onPick }: DevEUIParserProps) {
         const [raw, setRaw] = useState("");
         const [pick, setPick] = useState<"msb" | "lsb" | null>(null);
         const { data: preview } = useQuery({
           queryKey: ['parse-deveui', raw],
           queryFn: () => parseDevEUI({ raw }),
           enabled: raw.replace(/[^0-9a-fA-F]/g, '').length === 16,
         });
         // render Input (mono), preview rows, vendor hint
         // on confirm click: onPick(pick === 'msb' ? preview.msb : preview.lsb)
       }
       ```
       Validation: error "Enter a 16-character hex DevEUI." when not 16 chars after strip.

    5. **Create web/src/routes/devices/add-device-dialog.tsx** — 4-step Stepper dialog (D-10 + D-16):
       - Use ResponsiveDialog size="lg".
       - Use Stepper with steps `["Paste DevEUI", "Pick profile", "Bind to MP", "Review"]`.
       - Step 1: DevEUIParser; on pick → setDevEUI(eui); next.
       - Step 2: profile Select (lists from listProfiles); AppKey input with Eye/EyeOff toggle (mask); JoinEUI optional; OTAA read-only with hint per UI-SPEC.
       - Step 3: MP Combobox (shadcn command + popover); inline "+ Create new metering point" panel that mounts CreateMeteringPointDialog inline (per UI-SPEC §Step 3 inline create).
       - Step 4: Review — read-only summary rows (DevEUI mono, Profile, AppKey masked last-4, JoinEUI, MP); preflight ping triggered via `useQuery` on step entry (apiFetch /api/devices/preflight); StatusRow rendered for grpc + mqtt; if either err, submit disabled + alert per UI-SPEC. On submit: POST /api/devices; success → toast + onCreated + close.
       - Failure path inline `<Alert variant="destructive">` per UI-SPEC.

    6. **Create web/src/routes/devices/index.tsx** — minimal Devices list per D-29 + UI-SPEC. TanStack Table; columns Name, DevEUI mono, Profile, Bound MP, Last seen, Battery + semantic dot, Status. Default sort last_seen desc, page size 50. Search input. Add device button.

    7. **Create web/src/routes/metering-points/swap-meter-dialog.tsx** — 3-step Stepper dialog (D-12 + D-13 + D-14):
       - Use ResponsiveDialog size="lg" + Stepper steps `["Capture R", "Select new device", "Review math"]`.
       - Title: "Swap meter on {MP name}" — MP name passed as prop.
       - Step 1: query GET /api/metering-points/{id} → display "Outgoing reading: {R} {unit} — uplink {N min} ago" inside `<Card bg-secondary>`. If latest uplink > 1h old, switch to `<Alert variant="warning">` per UI-SPEC. Mandatory checkbox "I verified this matches the physical meter." Override link reveals editable input that adds warning chip on swap audit.
       - Step 2: device picker (reusable Combobox component built on shadcn command + popover; reuse from Add Device dialog if extracted as <DevicePicker>); filter to unbound devices by default with toggle. Inline `+ Add a new device` shortcut opens AddDeviceDialog stacked. New meter initial reading numeric input (default 0).
       - Step 3: math read-back panel `<Card>` with three labeled rows per UI-SPEC §Step 3 anatomy. Tooltip on "Show me the math" with copy verbatim from UI-SPEC. Optional override input. Submit "Confirm swap"; on submit → POST /api/metering-points/{id}/swap; success toast + close + invalidate MP detail query. 409 → inline alert "Concurrent uplink in progress — try again in a few seconds." per UI-SPEC.

    8. **Replace 3 vitest skip stubs** with the active tests above:
       - web/src/routes/devices/deveui-parser.test.tsx (Tests 1-4 from <behavior>)
       - web/src/routes/devices/add-device-dialog.test.tsx (Tests 1-3)
       - web/src/routes/metering-points/swap-meter-dialog.test.tsx (Tests 1-4)

    9. **Edit web/src/App.tsx** — add /metering-points/:id and /devices routes (lazy imports + path entries).
  </action>
  <verify>
    <automated>pnpm --dir web test routes/devices routes/metering-points --run && pnpm --dir web build</automated>
  </verify>
  <acceptance_criteria>
    - 8 .tsx files created (excluding tests): create-mp-dialog, $id (MP detail), swap-meter-dialog, devices/index, add-device-dialog, deveui-parser, devices/$id (omit if not in scope — Phase 4 owns full detail; we ship list only)
    - `grep -F "Stepper" web/src/routes/devices/add-device-dialog.tsx` returns at least 1 line (4-step Stepper used)
    - `grep -F "Stepper" web/src/routes/metering-points/swap-meter-dialog.tsx` returns at least 1 line (3-step Stepper used)
    - DevEUI parser uses parse-deveui endpoint: `grep -F "/api/devices/parse-deveui" web/src/routes/devices/deveui-parser.tsx` returns 1 line
    - Add Device dialog uses preflight + add: `grep -E "/api/devices/preflight|/api/devices'" web/src/routes/devices/add-device-dialog.tsx` returns at least 2 lines
    - Swap dialog uses swap endpoint: `grep -E "/api/metering-points/.*/swap" web/src/routes/metering-points/swap-meter-dialog.tsx` returns at least 1 line
    - All 11 tests across 3 files PASS: `pnpm --dir web test routes/devices routes/metering-points --run` exits 0 with 11+ tests passed (4 deveui + 3 add-device + 4 swap)
    - Vitest no longer reports 3 skipped tests in those files: `pnpm --dir web test --run 2>&1 | grep -E "skip"` returns at most 0 references to add-device-dialog/deveui-parser/swap-meter-dialog
    - `pnpm --dir web build` exits 0
    - App.tsx has /devices + /metering-points/:id routes: `grep -E "path: 'devices'|metering-points/:id" web/src/App.tsx` returns at least 2 lines
    - **W2 checkpoint:** Task 2 changes committed BEFORE the next checkpoint and BEFORE Task 3 begins. Full vitest suite (`pnpm --dir web test --run`) MUST be green at commit time.
  </acceptance_criteria>
  <done>MP detail + Devices list + 3 dialogs (Add MP, Add Device 4-step, Swap Meter 3-step) + DevEUI parser ship; 11 tests pass; Stepper reused per UI-SPEC; preflight + atomic add + swap-with-409 handling all wired. Task 2 commit landed; full vitest green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint B: Verify Task 2 commit + green full vitest before starting Task 3</name>
  <what-built>Task 2 — Metering Points + Devices surfaces: $id detail pages, 3 dialogs (Add MP / Add Device 4-step / Swap Meter 3-step), DevEUI parser reusable component, Devices list, 4 lib helpers (devices.ts / metering-points.ts / swap.ts), App.tsx routes for /metering-points/:id + /devices.</what-built>
  <how-to-verify>
    1. Confirm the Task 2 commit landed: `git log -1 --oneline` should show `feat(02-14): MP + Devices + 3 dialogs ...` (or similar).
    2. Run `pnpm --dir web test routes/devices routes/metering-points --run` — expect 11 PASS (4 deveui + 3 add-device + 4 swap).
    3. Run `pnpm --dir web test --run` (FULL vitest suite) — expect 0 fails AND 0 unexpected skips. The originally-stubbed test files for Tasks 1+2 (create-site-dialog, add-device-dialog, deveui-parser, swap-meter-dialog) are all flipped to active. Only mapping-editor.test.tsx remains skipped (Task 3).
    4. Run `pnpm --dir web build` — expect 0 errors.
    5. Optional visual: open http://localhost:5173/devices and click Add device → walk all 4 steps with mocked backend (or real if a stack is running). Confirm Stepper renders, DevEUI parser shows MSB+LSB rows.
    6. **CRITICAL — fresh-context recommendation**: per the W2 checkpoint protocol, the executor SHOULD `/clear` (start a fresh context window) before beginning Task 3. Task 2 introduced 11 components + 4 lib helpers; Task 3 needs its own ~50% context budget for the mapping editor + JSON flatten helper + 6 vitest tests. Continuing in the same context window risks degraded reasoning during Task 3 (per the quality degradation curve in get-shit-done planner doctrine).
  </how-to-verify>
  <resume-signal>Type "approved" to start Task 3 (recommend `/clear` first), or describe the failure.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Profiles list + Mapping editor (D-08 paste-JSON + click-leaf)</name>
  <read_first>
    - 02-UI-SPEC.md §Profiles list page + §Profile editor page (single-page editor — NOT a dialog; explicit UX-01 deviation per UI-SPEC)
    - 02-UI-SPEC.md §New field patterns (mapping table editable, JSON tree leaf-pick, capability chip group)
    - web/src/routes/profiles/mapping-editor.test.tsx (current placeholder — replace)
    - internal/profile/handlers.go (Plan 02-11 — list/get/create/update/decoded-sample shapes)
    - internal/profile/editor.go (ProfileSaveInput shape — fields the editor sends)
    - **Task 2 checkpoint recovery (W2):** if entering Task 3 in a fresh context window, run `git log -1 --stat` to identify the Task 2 commit's file set, then read the new components from that commit (add-device-dialog.tsx, swap-meter-dialog.tsx, deveui-parser.tsx, create-mp-dialog.tsx, MP $id, Devices index) to recover state. Then read the new lib helpers (devices.ts, metering-points.ts, swap.ts). Confirm `pnpm --dir web test --run` is green BEFORE editing any Task 3 files.
  </read_first>
  <files>web/src/lib/profiles.ts, web/src/routes/profiles/index.tsx, web/src/routes/profiles/$id.tsx, web/src/routes/profiles/mapping-editor.tsx, web/src/routes/profiles/mapping-editor.test.tsx, web/src/lib/json-flatten.ts, web/src/App.tsx</files>
  <behavior>
    - Test 1 (MappingEditor_LoadsExistingProfile): mocked GET /api/device-profiles/{id} returns axioma_w1 profile + 5 mappings → editor renders profile name editable + 5 mapping rows in TanStack Table.
    - Test 2 (MappingEditor_PasteJSONRendersTree): paste valid JSON `{"cumul": 12345, "battery": 87}` into the left pane textarea → POST /api/device-profiles/{id}/decoded-sample mocked to return leaves; tree renders 2 leaves clickable.
    - Test 3 (MappingEditor_ClickLeafPopulatesPointer): click leaf "/cumul" while a mapping row is focused → that row's json_pointer cell becomes "/cumul".
    - Test 4 (MappingEditor_AddRow_DefaultsScale1): click "+ Add mapping" button → new row appended with scale="1" placeholder.
    - Test 5 (MappingEditor_Save_PostsForCreate): unsaved profile (no id) → save click POSTs /api/device-profiles with body matching ProfileRequest shape; success toast "Profile saved. Codec synced to ChirpStack." (or "Profile saved." if codec_js empty).
    - Test 6 (MappingEditor_BadCapability_400_RendersInline): POST returns 400 with "invalid capability" → inline alert "Choose valid capabilities" rendered above mapping table.
  </behavior>
  <action>
    1. **API helpers** in web/src/lib/profiles.ts:
       ```ts
       export type Mapping = { json_pointer: string; target: string; scale: string; data_type: 'numeric'|'int'|'bool'|'text'; position: number };
       export type Profile = { id: string; slug: string; name: string; vendor: string; family?: string; capabilities: string[]; counter_modulus: number; region?: string; mac_version: string; codec_js?: string; cs_profile_id?: string; codec_js_synced_at?: string };
       export type ProfileWithMappings = { profile: Profile; mappings: Mapping[] };

       export async function listProfiles(): Promise<Profile[]> { ... }
       export async function getProfile(id: string): Promise<ProfileWithMappings> { ... }
       export async function createProfile(body: Profile & { mappings: Mapping[] }): Promise<{ id: string }> { ... }
       export async function updateProfile(id: string, body: ...): Promise<void> { ... }
       export async function previewDecodedSample(id: string, sample: any): Promise<{ leaves: { json_pointer: string; value: any }[] }> { ... }
       ```

    2. **Create web/src/routes/profiles/index.tsx** — list page per UI-SPEC. TanStack Table columns: Name, Vendor, Family, Capabilities chips (max 3 + "+N more"), CS sync status row, Last updated, Actions overflow (Edit / Archive). Default seed rows render same as user-created (no "system" badge per D-09). Header with "Create profile" button → /profiles/new.

    3. **Create web/src/routes/profiles/$id.tsx** — wrapper that loads profile by id (or falls through to "new" mode at /profiles/new). Uses MappingEditor with loaded data. The route is single-page (NOT a dialog) — UI-SPEC explicit UX-01 deviation.

    4. **Create web/src/routes/profiles/mapping-editor.tsx** — D-08 two-pane editor per UI-SPEC §Profile editor page anatomy:
       - Top bar: Back button "← Profiles" + Profile name editable inline + Save profile primary button right + Cancel ghost. Unsaved-changes dot when dirty.
       - Identity row: Vendor + Family + Counter modulus inputs. Helper text on counter modulus per UI-SPEC.
       - Capability checkboxes grid: 10 D-04 tokens (`cumulative`, `flow_rate`, `instant_power`, `battery`, `temperature`, `pressure`, `leak_detection`, `tamper_detection`, `multi_phase`, `power_quality`). Each is a Checkbox with label.
       - **Two-pane body**:
         - Left pane: `<Textarea>` "Paste sample decoded JSON above"; on change, debounced POST /api/device-profiles/{id}/decoded-sample (or for new profile use a temp endpoint or local flatten — backend handler accepts any UUID; for new profile pass a synthetic id or use the dedicated GET /api/device-profiles/decoded-sample endpoint without id — confirm Plan 02-11 actually scoped it under {id}; if so, decoded-sample preview only works AFTER first save. If that's a UX block, the executor may add a separate POST /api/device-profiles/preview endpoint OR perform the flatten client-side via a small flattenJSON helper). **Pick the client-side flatten path** — simpler, no extra endpoint. Add `web/src/lib/json-flatten.ts` with a deterministic flatten function matching RFC 6901 semantics (the same one the backend's flattenJSON helper from Plan 02-11 implements).
         - Each leaf in the tree is a `<button>` that on click sets the active mapping row's json_pointer. Highlight active row with `bg-primary/10 border-l-2 border-primary` per UI-SPEC.
       - Right pane: TanStack Table editable rows. Columns: json_pointer (mono input), target (Combobox: 10 canonical column targets + freeform `extra.<key>`), scale (numeric input default "1"), data_type (Select: numeric/int/bool/text), Actions (Trash2 icon button to delete row). Tab navigates left→right; Enter advances row.
       - "+ Add mapping" ghost button below table. Adds row with empty pointer + scale=1 + data_type=numeric defaults.
       - Codec_js textarea below mapping table — `font-mono`. Helper "Sync to ChirpStack on save".
       - Live preview footer card: 3 cols (Sample JSON snippet | Mapping rows that fired | Resulting measurement row).
       - Save handler: validate (zod), build ProfileRequest body, call createProfile or updateProfile. On 400 with "invalid capability" → inline alert per UI-SPEC error patterns. On 503 "ChirpStack not bootstrapped" → inline alert "ChirpStack tenant not bootstrapped. Open Settings → Test connection." On success: toast + invalidate profiles query + navigate to /profiles.

    5. **Replace web/src/routes/profiles/mapping-editor.test.tsx** with the 6 tests in <behavior>.

    6. **Edit web/src/App.tsx** — add /profiles, /profiles/new, /profiles/:id routes (lazy imports + path entries).
  </action>
  <verify>
    <automated>pnpm --dir web test routes/profiles --run && pnpm --dir web build && pnpm --dir web test --run</automated>
  </verify>
  <acceptance_criteria>
    - 4 .tsx files exist: profiles/index.tsx, profiles/$id.tsx, profiles/mapping-editor.tsx, profiles/mapping-editor.test.tsx (rewritten)
    - Mapping editor has the two-pane layout: `grep -F "lg:grid-cols-2\|grid-cols-2 lg:" web/src/routes/profiles/mapping-editor.tsx` returns at least 1 line (or check for the same UI-SPEC two-pane responsive token)
    - JSON flatten helper exists: `ls web/src/lib/json-flatten.ts` returns the file
    - All 6 mapping-editor tests pass: `pnpm --dir web test routes/profiles/mapping-editor --run` exits 0
    - Full Phase 2 vitest suite: `pnpm --dir web test --run` exits 0 with 0 skipped tests in the 5 originally-stubbed Phase 2 dialog test files (verifiable: `pnpm --dir web test --run 2>&1 | grep -c "skipped"` should be 0 or only Phase 1 unrelated skips)
    - `pnpm --dir web build` exits 0
    - App.tsx has /profiles routes: `grep -E "path: 'profiles'|profiles/:id|profiles/new" web/src/App.tsx` returns at least 2 lines
    - Reference seed profiles render: when listProfiles is mocked to return [axioma_w1, acrel_adl200, acrel_adw300], the table displays all 3 with no "system" badge (test assertion in profiles/index test if added; OPTIONAL — primary acceptance is the 6 mapping-editor tests).
  </acceptance_criteria>
  <done>Profiles list + single-page editor (UI-SPEC explicit UX-01 deviation honored) + D-08 paste-JSON + click-leaf + TanStack Table mapping rows + codec_js textarea + capability checkboxes; 6 tests pass; full Phase 2 frontend suite green; bundle still builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/* | All Phase 2 endpoints behind session cookie auth (Phase 1 SCS); same-origin; CSRF defense via SameSite=lax cookie default |
| Browser localStorage / sessionStorage | NOT used for any auth or sensitive data — Phase 1 contract preserved |
| Mapping editor → backend | Codec_js paste from operator → POST /api/device-profiles → SaveProfile validates 256KiB cap + decodeUplink presence (Plan 02-08); UI does not need to re-validate but DOES truncate display for huge pastes |
| DevEUI parser → backend | Parse-deveui is a pure string transformation; POST body trivially validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14-01 | Information disclosure | AppKey shown in Add Device step 4 review | mitigate | Mask all but last 4 chars per UI-SPEC; Eye/EyeOff toggle for entry only; never persist to localStorage |
| T-02-14-02 | XSS | Operator pastes malicious JSON in mapping editor | mitigate | React's default JSX escapes string content; codec_js textarea is plain `<textarea>` — never `dangerouslySetInnerHTML`; preview footer renders values via `{JSON.stringify(...)}` |
| T-02-14-03 | DoS | Operator pastes 100MB sample JSON | mitigate | Client-side flatten caps at 10K leaves OR rejects >1MB paste before parse with toast "Sample too large — paste a representative subset" |
| T-02-14-04 | Tampering | URL parameter injection on /metering-points/{id} | accept | React Router escapes path params; backend re-validates as UUID; 400 surfaces gracefully |
| T-02-14-05 | EoP | Viewer sees admin-only buttons | mitigate | Use the existing Phase 1 useUser() hook + role check to hide Add/Edit/Archive/Decommission/Swap buttons for viewers (server still enforces 403; UI hide is UX, not security) |
| T-02-14-06 | DoS | Mapping editor's debounced decoded-sample POST floods backend | mitigate | useQuery enabled gate + 500ms debounce + maxLength=200 leaves on backend; UI blocks submit while preview pending |
| T-02-14-07 | Information disclosure | Sample JSON pasted into editor leaks PII to backend | accept | The decoded-sample endpoint is admin-only; sample JSON never persists server-side; transient request-response only |
</threat_model>

<verification>
After all 3 tasks (with checkpoints A + B between them):

1. `pnpm --dir web build` exits 0 (full SPA bundles)
2. `pnpm --dir web test --run` exits 0 with 0 Phase 2 skips remaining
3. `pnpm --dir web typecheck` (or `tsc --noEmit`) clean
4. `pnpm --dir web lint` (existing biome / eslint config) clean — no new lint errors introduced
5. `git ls-files web/src/routes/sites/ web/src/routes/devices/ web/src/routes/metering-points/ web/src/routes/profiles/` lists at least the 11 .tsx files this plan created
6. Sidebar nav order matches UI-SPEC §Layout — Sites, Devices, Profiles, Settings (verifiable: `grep -E "Sites|Devices|Profiles|Settings" web/src/components/shell/sidebar.tsx | head -10` lists them in that order)
7. Three commits landed (W2): Task 1 commit + Task 2 commit + Task 3 commit (verify with `git log --oneline | grep "02-14"` — should return 3 lines)
</verification>

<success_criteria>
- All 5 originally-skipped vitest stubs flipped to active tests; total ~25 net-new frontend tests passing
- Operator can navigate /sites → click Add site → fill form → see new site in list
- Operator can navigate to MP detail → click Swap meter → walk 3 steps → see math read-back → confirm → measurement chart (Phase 4 will render the chart; Phase 2 just the cumulative number) updates
- Operator can navigate /devices → click Add device → walk 4 steps → see preflight statuses → submit → device appears in list
- Operator can navigate /profiles → click profile → edit mapping rows + paste sample JSON → click leaves → save → CS sync confirmation
- Sidebar shows MapPin / Cpu / Layers icons for the 3 new nav items
- Phase 1 surface unchanged (login, install wizard, settings, account menu, change password all still work)
- W2: 3 commits land (one per Task), each preceded by a green vitest baseline; Tasks 2 + 3 begin from clean (and ideally fresh-context) state
</success_criteria>

<output>
After completion, create `.planning/phases/02-domain-model-canonical-schema/02-14-SUMMARY.md` documenting:
- Files created (~11 .tsx components + 5 test files + 4 lib helpers)
- shadcn primitives added (6 — table, command, popover, radio-group, textarea, scroll-area)
- Test count + pass status (~25 net-new tests; 5 skip placeholders flipped)
- Sidebar nav order verification snippet
- App.tsx route table (snippet of all Phase 2 routes added)
- W2 outcome: 3 commits landed (Task 1 + Task 2 + Task 3); each commit message + SHA
- Note for Plan 02-15: VALIDATION.md component-level rows for SITE-01 / DATA-04 / DATA-09 / CHIRP-04 dialogs can flip from ⬜ pending to ✅ green pointing at the new test files
</output>
